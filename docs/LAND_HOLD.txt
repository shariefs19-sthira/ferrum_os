# LAND_HOLD.txt — branches scripts/land.ps1's catch-all loop must skip.
#
# One glob per line. Blank lines and lines starting with # are ignored.
# Matching uses PowerShell -like semantics (e.g. "w2-234/*" matches any
# branch under that prefix). Add a branch here when it's intentionally
# held back from the catch-all — a WIP branch not ready to land, an
# experiment, anything you don't want swept in automatically. A targeted
# `git merge --squash origin/<branch>` still works on a held branch;
# only the catch-all loop in land.ps1 skips it.
#
# Remove a line (or the whole file) once the branch is ready to land
# normally, or has already landed via a targeted merge.

w2-234/*

# 2026-09-01: legacy pre-Relume branches, held pending triage. W2-265 fixed
# land.ps1's false-skip bug so it now correctly recognizes these as having
# "unique" content vs main — but that's because main has since moved past
# them (route restructuring, page rewrites), not because they carry real
# new value. Landing w2-30/copilot-designstudio, w2-25/cline-layouts2, and
# w2-27/cline-layouts3 in the same run W2-265 landed reintroduced 5 stale
# pre-move files at retired routes (apps/web/app/{designstudio,investflow,
# promarket,communitybuild,procurehub}/*) — reverted in 5fc9e4f/0b3d80a/
# fda5be4. Holding the rest of this era's branches until each is reviewed
# for genuine unique content vs. staleness, one at a time, rather than
# risking the same regression again via the catch-all sweep.
w2-08/*
w2-10/*
w2-16/*
w2-18/*
w2-20/*
w2-22/*
w2-23/*
w2-24/*
w2-25/*
w2-26/*
w2-27/*
w2-28/*
w2-29/*
w2-30/*
w2-31/*
w2-33/*
w2-34/*
w2-35/*
w2-36/*
w2-38/*
w2-40/*
w2-41/*
w2-42/*
w2-43/*
w2-44/*
w2-45/*
w2-46/*
w2-48/*
w2-49/*
w2-50/*
w2-51/*
w2-52/*
w2-53/*
w2-54/*
w2-62/*
w2-63/*
w2-64/*
w2-65/*
w2-67/*
w2-69/*
w2-75/*
w2-79/*
w2-80/*
w2-86/*
w2-90/*
w2-94/*
