# TASK_BOARD.md — Pull-queue (AGENTS.md RULE 35)

Seeded 2026-09-04. While AGENTS.md RULE 34 (single-outcome focus) is in
effect, every row here is a Workspace row — everything else stays
DEFERRED-per-RULE-34 in docs/WAVE_QUEUE.md, not on this board at all.

Per RULE 35(2): a seat claims the top READY row it's eligible for whose
deps are DONE and whose envelope overlaps no CLAIMED row, executes,
marks DONE with SHA + live proof, then immediately pulls next. Per
RULE 35(4): seats update this board only on DONE or STUCK.

**PRODUCT tags (RULE 57, adopted 2026-09-08):** every row carries a
PRODUCT tag inline in its Acceptance text — one of LandIntel |
DesignStudio | Structura | BOQPro | ProMarket | ProcureHub | InvestFlow
| BuildOS | CommunityBuild | Transact | CROSS (spans more than one
product). Tagging is applied incrementally, not retroactively to every
historical row in one pass — see the Notes section for which rows are
tagged so far and the plan for the rest.

| ID | Title | Envelope (files) | Eligible seats | Acceptance | Deps | Status |
|----|-------|-------------------|-----------------|------------|------|--------|
| W-26 | ROUTING_FLIP | `apps/web/app/project-workspace/page.tsx` (route swap: cockpit becomes the default), new `apps/web/app/project-workspace/projects/page.tsx` (demoted project-list shelf) | CRANE | **TOP OF BOARD — nothing outranks it, per the operator (2026-09-04); placed as this table's first row for that reason, not appended at the end.** `/project-workspace` renders the cockpit immediately: a preview session is auto-created (per W-17's preview-mode convention, `localStorage` flag, zero credential collection) so the cockpit has a real project to render into rather than an empty state; all five sketch regions (top-strip tabs, right tools ruler, center 3D space, bottom-left more-tools drawer, bottom-full-width extract panel) are visible in one viewport. The project list (previously at `/project-workspace`) demotes to `/project-workspace/projects`. Acceptance: opening `/project-workspace` shows tabs + ruler + 3D space + extract panel + command bar together in one viewport, verified at both 1366 and 375 on the deployed edge. | — | DONE — `86ef5791` ("[land:crane/w26-cockpit-route-flip]"), touching exactly the two files this row's envelope names. **Status reconciliation (2026-09-04):** landed status confirmed against `origin/main`; RULE 25 deployed-edge live verification is a separate, still-open concern SCRIBE has not itself performed — this DONE marks landing, not a live-proof claim. |
| W-22 | ARCHVIZ_GRAPHICS (superseded 2026-09-08 — see pointer below) | the 3D configurator's rendering pipeline (center viewport per docs/UX_FLOW.md phase 3) | MASON | **RECONCILED 2026-09-08 (PI's first Execution Ledger review found this row cited in docs/ACTIVITY_LOG.md — commit `97c1d47f8`, 2026-09-04 — but absent from the current board; restored here per RULE 3 append-only, not silently dropped a second time.** Original ask: multiple viewports (plan + axonometric), a PBR material set, an IBL sun, a reflective podium surface, instanced greenery, within a stated fps budget, referencing docs/UX_FLOW.md phase (3)'s center-viewport description; depended on `three` (W2-380) and an unseeded W-21 (never seeded — flagged honestly in the original text, not fabricated). **Superseded by W-92 PHOTOREAL_VIEWPORT (2026-09-07):** W-92's scope is a strict superset of this row's original ask — PBR materials, HDR sky/IBL-equivalent lighting + reflections, textured ground (podium-equivalent), vegetation, and device-tiered fps guard all cover this row's requirements at a more complete spec. No independent work remains under W-22's own name; pull W-92 instead. | W-92 | SUPERSEDED — see W-92, no independent work remains |
| W-23 | NODE_PANEL | n/a — roadmap only, no envelope claimed | (unassigned) | **RECONCILED 2026-09-08 (same PI finding as W-22, restored per RULE 3, not dropped again).** Dynamo-style visual parametrics node panel. **Status: ROADMAP-LABEL, as originally given** — not pull-eligible under RULE 35; recorded on the board because the operator named it with a board-style ID. No newer row has superseded this concept — it remains a real, un-superseded roadmap item, distinct from W-22's fate. | — | ROADMAP-LABEL |
| W-24 | COMPLIANCE_ENGINE | new lib module (parcel + building type → permissions ruleset engine) | CRANE or MASON | **RECONCILED 2026-09-08 (same PI finding — this is the most consequential recovery: at least a dozen other board rows, including W-27/28/53/58/88/91, cite "W-24" as a dependency, and none of them had a real row to point to.** Deterministic ruleset: given a parcel + building type, returns a permissions list where each item carries authority, stage (BUY or BUILD), an indicative timeline/fees figure, and a documents checklist — sourced from the existing `2026.1-SAMPLE` versioned ruleset (`apps/web/lib/parcelIntel/sampleRulesets.ts`, confirmed still present on disk directly before restoring this row, not assumed). Unit-tested against known-good/known-bad vectors. Every output item honesty-labeled INDICATIVE (matching the sample ruleset's own version tag) per RULE 5/29 — no permission item may read as verified/current-government-rate when derived from the 2026.1-SAMPLE set. Confirmed via direct disk check that no compliance-engine module exists yet anywhere in `apps/web/lib/` — this row's work is genuinely still undone, not silently completed under a different name. | — | READY |
| W-25 | PERMISSIONS_TABS | cockpit top-strip tabs (originally: two new slots, DILIGENCE + PERMITS); bottom extract panel (per-tab compliance table render) | RIVET or MASON | **RECONCILED 2026-09-08 (same PI finding), but flagged rather than blindly restored as originally written:** this row's original text assumed two NEW top-strip tab slots named DILIGENCE and PERMITS. The cockpit's tab structure has since solidified around a fixed set of ten tabs (Land/Design/Structure/Cost/Market/Procure/Invest/Build/Community/Transact — per W-59's persona table and W-81's per-tab skins), with no DILIGENCE or PERMITS slot among them. **This is a real, unresolved conflict, not silently resolved either way:** whoever picks up this row must decide whether W-24's compliance-table content (per stage: BUY vs. BUILD) surfaces as a view/filter WITHIN the existing Land tab (buy-stage) and Cost or Build tab (build-stage) rather than as two additional top-strip slots, or whether the ten-tab structure gets an explicit, operator-approved eleventh/twelfth slot — this row does not pre-decide that, since RULE 44/RULE 1's ten-tab convention is itself a significant existing commitment this row's original text predates. Depends on W-24 existing to have real data to render. | W-24 | READY (scope conflict flagged above — resolve tab placement before or during implementation) |
| W-83 | MOBILE_SHELL_DEAD_URL [PRODUCT: CROSS] | `apps/mobile/capacitor.config.json`, `apps/mobile/android/app/src/main/res/xml/network_security_config.xml`, `apps/mobile/android/app/src/main/assets/capacitor.config.json` | RIVET | **LIVE DEFECT, found during SCRIBE's own board/docs stewardship pass, not reported by any seat — confirmed directly on disk, not assumed:** the Android Capacitor shell (W2-356, RIVET's exclusive `apps/mobile/**` path) is still hardcoded to the retired `https://ferrum-os.shariefsatyala.workers.dev` host, dead since the 2026-09-05 worker rename (per docs/FLEET_SEATS.json's `deployment` block) — `capacitor.config.json`'s `server.url` and `server.allowNavigation`, plus the Android network-security-config's allowed domain, all still name the dead host. Since `network_security_config.xml` restricts cleartext/allowed hosts to exactly this one dead domain, the shipped app currently cannot reach the live edge at all — this is not a cosmetic staleness, it is a broken mobile app. Fix: update all three files (plus the built `android/app/src/main/assets/capacitor.config.json` copy, which must match the source config, not diverge from it) to `ferrumos-preview.shariefsatyala.workers.dev`, per docs/FLEET_SEATS.json's `deployment.liveUrl` — read from that single source of truth (RULE 51 applies: this rename is now automatable via a config read, not a manual per-file edit if any future rename happens again). Acceptance: all three files reference the current live URL, none reference the dead one; a real Android build/emulator run reaches the live app content, verified directly, not assumed from config text alone. | — | READY |
| W-84 | PREVIEW_FULLBLEED | preview/embed section CSS on all ten product pages (`apps/web/app/products/*/page.tsx` and the shared `ProductCockpitPreview.tsx` component) + the workspace cockpit route's equivalent container | (any seat — owner-agnostic, TOP OF QUEUE per the operator) | **Amends W-47 (see that row's own amendment note) — this row's full-bleed requirement supersedes any percentage-based canvas-sizing target on the ten product-page preview embeds specifically; the cockpit route keeps W-47's own ≥70%/≥60% figures.** Every cockpit/preview embed on all ten product pages renders full-bleed: the canvas consumes 100% of its containing section's width, INCLUDING the currently-empty left gutter that today's layout leaves dead — no `max-width` wrapper constrains the preview section, and no side padding/margin eats into the canvas's available width. Height is at least 70vh on every one of the ten pages plus the workspace route. The data-extract panel becomes an overlay or side panel that never squeezes the canvas to make room for itself — it floats/docks on top of or beside the full-width canvas, not inside a shrunk column next to it. **Acceptance is measured, not eyeballed:** on all ten product pages plus the workspace route, a headless check confirms the canvas element's `clientWidth` exactly equals its containing section's `clientWidth` (not "close to," not "visually full-width" — an exact pixel match); screenshots at 1366 and 1920 show zero dead/empty left gutter on any of the eleven routes; height is confirmed ≥70vh on each. | W-47 | READY |
| W-85 | LAND_DETECT_IN [PRODUCT: LandIntel] | LandIntel lookup UI + Land tab + Territory panel (per RULE 44) — extends W-65's envelope; categorical-control/slider cleanup on the same surfaces — extends W-53's envelope | (any seat — owner-agnostic, TOP OF QUEUE per the operator, after W-84) | **Amends W-65 (see that row's own amendment note) — extends its three input modes to the full six ways land is actually detected online in India.** All six on LandIntel, the Land tab, and the Territory panel (RULE 44, all three per the operator's own instruction): (a) **MAP PIN DROP** — click anywhere on the map to set the location; the dropped point reverse-geocodes to a real address line, shown back to the user, not left as bare coordinates. (b) **GPS COORDINATES** — direct decimal lat/lon entry plus DMS parsing (per W-65, carried forward unchanged). (c) **PLACE/ADDRESS search** — Nominatim typeahead (per W-65, carried forward unchanged). (d) **USE-MY-LOCATION** — a button invoking the browser Geolocation API, permission-gated (an honest permission-denied state, never a silent failure or a fabricated location). (e) **ULPIN/Bhu-Aadhaar** — the existing official lookup (per W-65, carried forward unchanged); seeded now, live registry integration stays later. (f) **Survey/khasra/plot-number per state** — explicitly a ROADMAP-labeled mode, not built in this pass; recorded with its own provenance note (each state's survey-numbering scheme differs and needs its own adapter) rather than silently omitted. On any resolve (any of modes a–e): the map recenters, the OSM drape re-renders at the new bounding box (reusing W-51's existing adapters, no new rendering path), the site's parcel context is set, and a provenance chip shows method + source + date (e.g. "Pin drop · reverse-geocoded · 2026-09-07") — every resolve is traceable to how the location was actually obtained, not a single generic "location set" state. **CLEANUP, same row, same pass (amends W-53):** on these surfaces, categorical/discrete controls (e.g. a fixed set of location-method choices) render as chips or selects, never as a slider standing in for a categorical choice; every genuine numeric slider that remains keeps W-53's existing paired-numeric-input pattern (metric + imperial where applicable) PLUS a live value readout and visible units at all times, not just on interaction. **Default map center:** on first load with no location yet resolved, the map centers on a disclosed sample city (Bengaluru) with an explicit "SAMPLE LOCATION" chip — never an unexplained/stray pin with no context. Acceptance: screenshots showing pin-drop resolving to a reverse-geocoded address, coordinate entry, place search, and the browser's geolocation permission prompt actually firing; a headless scan finds zero bare `<input type="range">` with no paired numeric input on this page; the default (no-resolve) state shows the pin on Bengaluru with the sample-location chip visible, not an unexplained pin. | W-65, W-53, W-51 | READY |
| W-88 | PRECISION_CONTROLS [PRODUCT: CROSS] | control-component logic (numeric field + unit toggle + stepper + detented slider state) — MASON's piece; visual styling (monospace values, tick marks, tooltip) — RIVET's piece | MASON (logic) + RIVET (styling) | **Top of queue (2026-09-07, with W-89), row-number gap (W-86/W-87 not used) is the operator's own numbering, not an omission SCRIBE introduced.** Replaces every basic slider fleet-wide with a technical instrument-grade composite control: a numeric input field, a unit toggle (m/ft, per RULE 30's existing dual-unit convention), a stepper (+/- buttons for fine adjustment), and a detented slider carrying visible tick marks at the ruleset's actual code-derived limits (per W-24 COMPLIANCE_ENGINE) — not evenly-spaced decorative ticks. A live value tooltip follows the slider thumb during drag. Every part of the composite is keyboard-adjustable (arrow keys, not mouse-only), and displayed values render in a monospace font for precise digit alignment. This extends, not replaces, W-53's slider+paired-numeric-input pairing (this row's numeric field IS that paired input, upgraded with the unit toggle/stepper/detents/tooltip on top) and W-85's stricter live-value+units requirement on the Land/Territory surfaces. **Dead-binding audit, same row:** every existing control on the fleet is checked for a dead `range`/`step`/`onChange` binding (a control that renders but doesn't actually respond to input or doesn't feed the intent pipeline) and fixed as part of this pass, not deferred to a separate row. Acceptance: a headless interaction test exists per control and confirms it actually responds (value changes propagate through the intent pipeline, matching W-53's shared-pipeline requirement); screenshots of the new control styling on the Land and Cost surfaces specifically. | W-24, W-30, W-53 | READY |
| W-89 | NO_AUTONOMOUS_MUTATION | idle/demo-mode logic (`SutraPanel.tsx`'s demo cycle and any equivalent idle-loop code) + an engine-level floor-count hard cap | MASON | **Top of queue (2026-09-07, with W-88).** The idle/demo mode may NEVER mutate real project state. It is restricted to camera moves (orbit, pan, zoom — consistent with RULE 50's existing view-only mouse framing) and highlight sweeps (visually cycling attention across elements without changing any of them), OR it runs entirely on a clearly-labelled DEMO scratch model that is never the user's real project — never a silent mutation of the actual project state the user is looking at. All genuine state changes originate from real user intent only: SUTRA text, voice, option chips, or W-58's questionnaire fallback. **Citation note:** the instruction cites "RULE 49/50" — RULE 49 is intentionally unassigned (a numbering gap, established at RULE 50's own adoption, not an omission here); the operative rule for this row's view-only-idle framing is RULE 50 alone. **Engine-level hard cap, enforced in the engine not the UI:** floor count is capped at `min(requested, ruleset max_height ÷ floor height)` — a UI-only cap is insufficient, since it wouldn't stop a runaway value reaching the engine through any other path (SUTRA intent, a preset, a future API). Any floor count found to already exceed this cap (a runaway state from a prior bug) is reset to the capped value as part of this row's own landing, not left in place for a future pass. Acceptance: leaving the cockpit idle for 60 seconds and hashing the project state before/after shows an unchanged hash (byte-for-byte, not "looks the same") — the definitive test that idle/demo never mutates; attempting to set a floor count above the engine-enforced cap (via any input path, not just the UI) is rejected or clamped at the engine level, verified directly against the engine's own output, not the UI's display. | W-58, W-24, W-50 | READY |
| W-87 | REGIONAL_KB [PRODUCT: CROSS] | new KB data module (regional/typological aesthetic-recommendation tables) — extends W-29/W-41's existing KB modules | CRANE | **Backfilled 2026-09-07: this row number was left as an intentional gap when W-88/W-89 were seeded (consistent with the RULE 15/32/49 gap convention), but W-91's own instruction cites "W-87 regional KB" with real, usable content — per RULE 27/39 that supplied content is used to seed this row for real now, rather than left as a second unexplained gap or fabricated beyond what was actually given.** A KB data module carrying region/typology-specific AESTHETIC RECOMMENDATIONS (distinct from the hard regulatory minimums W-24/W-29 already carry — this module is advisory, never a compliance gate): coastal wind gaps (open-space guidance for coastal typologies, wind-flow-driven, not code-mandated), hill terracing (setback/level guidance for sloped-site typologies), courtyard ratios (traditional courtyard-to-built-area proportions for typologies where that pattern applies), and other region/typology pairs as the KB grows — each entry carries a status chip (per W-29/W-41's existing VERIFIED-SAMPLE/INDICATIVE convention) and is explicitly labeled advisory/aesthetic, never presented as a code requirement (the same honesty distinction RULE 44/W-41 already draws for Vaastu). Acceptance: at least the three named region/typology entries (coastal wind gaps, hill terracing, courtyard ratios) are seeded with real content and a status chip; each is consumed by W-91 as a labeled "aesthetic recommendation," never merged into or confused with the regulatory-minimum figures on the same screen. | W-29, W-41 | READY |
| W-91 | OFFSETS_PANEL [PRODUCT: CROSS] | new "Open Space & Offsets" panel (three-authority-tab UI + exact-entry inputs) — MASON's logic piece; instrument styling per W-88 — RIVET's piece; replaces the deleted "How much open ground?" slider (`Space3D.tsx` or wherever that control currently lives) | MASON (logic) + RIVET (styling) | **Replaces the "How much open ground?" slider entirely — the slider is deleted, not hidden or demoted, per the operator's own instruction.** A new OPEN SPACE & OFFSETS panel with: (1) **THREE AUTHORITY TABS** — NBC 2016 | State DCR | Local body — each tab shows that specific authority's recommended minimum open space and front/rear/side setbacks for THIS plot, computed live from the plot's actual area/height/occupancy (per W-24 COMPLIANCE_ENGINE) with a clause citation from the KB (per W-29/W-41) attached to every figure shown — a number with no citation is not acceptable output for this panel. (2) **AESTHETIC RECOMMENDATION line**, per the plot's location/typology, sourced from W-87 REGIONAL_KB (coastal wind gaps, hill terracing, courtyard ratios, etc.) — visually and textually distinct from the three regulatory tabs, labeled as advisory, never presented as a fourth authority or a compliance requirement. (3) **USER EXACT ENTRY** — precision numeric inputs, per W-88's instrument-grade control pattern (numeric field + paired unit toggle, keyboard-adjustable, monospace), for open space and each of the four offsets individually. Entering a value below any authority's stated minimum shows a warning chip citing the specific clause that value falls short of — it NEVER blocks entry; the user's actual plan can still proceed with the warning visible, since some rows genuinely need an exception path and this panel does not adjudicate that. **RULE 44 (seeded pre-generalized):** this panel and its three-tab/aesthetic-line/exact-entry structure is the single source for open-space/offset input everywhere the deleted slider used to appear, not just one surface. Acceptance: screenshots of all three authority tabs, each showing real citations, for a Bengaluru sample plot; entering an offset value below the displayed minimum on any tab produces a warn chip naming the specific clause; a sitewide scan confirms zero remaining open-ground/open-space sliders anywhere. | W-24, W-29, W-41, W-87, W-88 | READY |
| W-92 | PHOTOREAL_VIEWPORT [PRODUCT: CROSS] | rendering pipeline (materials, lighting, sky, tone mapping, post-processing, quality-tier system) on the existing three.js cockpit canvas | MASON | **Top of queue, after W-89/W-88/W-91 per the operator's own ordering.** The cockpit stops rendering flat/untextured boxes and renders like a product-grade visualization tool: (1) **PBR material library** — physically-based materials for concrete, glass, brick, plaster, wood, and tile, applied to the model's actual elements (per W-66's element set), not a single flat-shaded default material reused everywhere. (2) **Sun position** computed from the plot's real latitude/longitude (per W-51/W-65/W-85's resolved site context) and the current time-of-day — not a fixed, arbitrary light direction. (3) **HDR/procedural sky** plus environment reflections, so reflective materials (glass) show a real environment, not a flat color. (4) **ACES tone mapping** for filmic, non-blown-out highlights. (5) **SSAO (screen-space ambient occlusion) + soft shadows**, replacing hard-edged or absent shadows. (6) **Textured ground, materialized context buildings, and vegetation** — the site context (per W-51/ONE-GROUND's OSM-derived buildings) is no longer flat gray geometry. (7) **Quality tiers, auto-selected by device capability** (reusing W-41(2)/W-34's existing WebGL2-full vs. WebGL1/degraded capability-detection mechanism, not a new detection path), with an fps guard that steps the tier down rather than letting frame rate collapse silently. Acceptance: before/after screenshots shown side by side make the visual upgrade unambiguous, not a subtle tweak; the rendered shadow direction is verified to actually match the resolved site's real latitude/longitude and time-of-day (a computed check, not eyeballed); an fps assertion passes green on the mid-tier device profile defined in RULE 41's floor-device protocol, confirming the quality-tier/fps-guard system actually holds the frame budget, not just that it exists in code. | W-51, W-66, W-34, W-41 | READY |
| W-94 | PREFERENCE_TELEMETRY [PRODUCT: CROSS] | consent UI + on-device minimization/anonymization pipeline + labelled-event capture wiring (SUTRA intents, questionnaire paths, post-generation edits, overrides, acceptances) | MASON | **Consent-first, DPDP Act 2023-compliant by design, not retrofitted.** Consent UI is opt-in — no capture of any kind happens before the user actively consents, and declining consent must not degrade or gate the product experience (consent-for-a-discount/consent-for-a-feature patterns are explicitly out of scope, per DPDP's own consent-must-be-freely-given principle). Sensitive fields (age group, language, region, ethnicity) are OPTIONAL to provide, and are minimized and anonymized ON-DEVICE into cohort buckets before anything leaves the client — raw PII in any of these fields NEVER reaches D1; only the bucketed cohort identifier does. Every SUTRA intent, every questionnaire path taken (per W-58), every post-generation edit, every override of a system suggestion, and every acceptance becomes a labelled training event — labelled with the cohort bucket (if consented) and the event type, stored for W-95's consumption. **RULE 44 (seeded pre-generalized, per the operator's own instruction):** the consent UI and its opt-in/optional/on-device-minimization contract apply to every surface that could capture this data, not a single consent dialog bolted onto one page. **Acceptance amended 2026-09-07, per ATLAS's real, landed `docs/FERRUM_AI_ARCHITECTURE.md` finding (Part 2, DPDP consent):** DPDP Act 2023 consent is purpose-specific — "no secondary use or repurposing without fresh consent" is the Act's own confirmed text, and Ferrum's original "help me design my building" consent does NOT automatically cover "use my choices to train a model for other users." This makes the W-95 learning loop unlawful by design unless the consent text this row builds explicitly names the actual purpose: the consent UI's copy MUST state, in plain language the user actually reads, that opting in permits use of their interaction data for "improving generation models" (or equivalent explicit wording) — a generic "help us improve" or "personalize your experience" line is NOT sufficient under this finding, since it doesn't name the specific model-training purpose DPDP requires be disclosed. **Cohort priors stay flagged unverified until measured:** any claim this row or W-95 makes about a specific cohort's preference ("coastal Kerala cohorts prefer X") is explicitly INDICATIVE/unverified until actually derived from real aggregated consented data — never asserted as established fact before that measurement exists, consistent with W-95's own volume-gated-prior requirement. Acceptance (superseding/extending the acceptance below): the consent UI's actual copy is checked to contain an explicit "improving generation models" (or equivalent, real, non-generic) purpose statement, not just a generic-consent screenshot; every cohort-prior figure surfaced anywhere before real measurement exists carries a visible unverified/indicative label, checked directly, not assumed compliant from the UI design alone. Acceptance (original, still in force): declining consent is verified to leave every other product surface fully functional (a real negative test, not assumed); a sensitive-field submission is verified to never appear as raw PII in D1 — only the cohort bucket does, checked directly against the actual stored row; a real SUTRA intent, questionnaire completion, edit, override, and acceptance each produce a real labelled event, verified by inspecting the stored event, not by code inspection alone. | W-27, W-58, W-96 | READY |
| W-95 | LEARNING_LOOP [PRODUCT: CROSS] | feedback-label aggregation pipeline (acceptance/edit-distance/override → cohort priors) + quality-metrics computation + a visible quality dashboard | MASON | Consumes W-94's labelled events and aggregates them into cohort priors — a real, computed statement like "coastal Kerala cohorts prefer X" derived from actual acceptance/edit-distance/override data for that cohort bucket, never an asserted or hardcoded preference. Computes quality metrics from the same event stream: audit pass rate (per W-55's PLAN_AUDIT), acceptance rate, and edit distance (how much a user's post-generation edit diverges from the system's original suggestion) — each metric shown on a visible quality dashboard, not buried in logs only an engineer can read. **Retrain cadence is gated on data volume**, not a fixed calendar schedule — a minimum labelled-event count (or equivalent volume threshold) must be met before any retrain/re-derivation of cohort priors runs, so a thin, early data set doesn't produce an overconfident or noisy prior. Acceptance: the quality dashboard shows real, currently-computed values for audit pass rate/acceptance rate/edit distance (not placeholder numbers); at least one real cohort-prior statement is derived from actual aggregated W-94 event data and traceable back to that data, not asserted; the volume gate is demonstrated by showing a below-threshold cohort correctly withholding a prior (or flagging it as low-confidence) rather than producing one anyway. **Amended 2026-09-07 (per ATLAS's `docs/FERRUM_AI_ARCHITECTURE.md` finding, cross-referenced from W-94):** this row's aggregation is lawful only against data collected under W-94's amended, purpose-specific consent (explicitly naming "improving generation models") — this row does not independently re-verify consent scope, W-94 owns that gate; every cohort-prior statement surfaced anywhere, including on this row's own dashboard, carries an unverified/indicative label until it is actually derived from real measured data at or above the volume gate, never presented as established preference before that. | W-94, W-55, W-96 | READY |
| W-96 | FERRUM_AI_ARCHITECTURE | research/spec document (two-head self-learning design + staged rollout plan) — no source code envelope, ATLAS research row | ATLAS | A research/spec row, not an implementation row: documents the two-head self-learning architecture — a TECHNICAL head (trained/derived from the KB, generated plans, and their audit scores) and a PREFERENCE head (trained/derived from cohort embeddings per W-94/W-95) — and the staged rollout that gets there without skipping steps: (1) deterministic today (the existing rule-engine/KB-cited approach, no ML in the decision path); (2) cohort priors next (W-95's aggregated, volume-gated priors informing suggestions, still not a trained model making decisions); (3) fine-tuned open models at volume (only once W-95's data volume genuinely supports it, and explicitly gated on W-31 GROUNDED_LLM_LAYER's own not-yet-approved LLM-seat trial — this stage does not start before that gate clears); (4) distributed training later, referencing the PrimeIntellect concept spec (W-78) as a future infrastructure option, not a commitment to build it now. Acceptance: the spec document exists, names the two heads and their actual data sources (not invented ones), and states all four stages with their real gating conditions (W-31 for stage 3, data volume for stages 2 and 3) rather than presenting the roadmap as unconditional. | W-94, W-95, W-31, W-78 | READY |
| W-97 | LANDEED_COMPETITIVE_FOLLOWUP [PRODUCT: LandIntel] | research continuation — no source code envelope, ATLAS research row | ATLAS | **Seeded 2026-09-08: `docs/LANDEED_DEEP_DIVE.md` (real, landed ATLAS research, `6fd837e9b`) had no board row — read in full before seeding, not assumed from its title.** The existing research inventories Landeed's five product lines (Document Retrieval, Document Fetch API, AI Title Reports/"Terra Black," Encumbrance Monitor, Registration Services, Bill Payments, Pulse market-intelligence, LEX) and 23+-state coverage claim (not independently quantified per-state), but flags one real, unfilled gap in its own text: `web.landeed.com`'s live search flow timed out under automated navigation and was never walked to a sample-report output this pass. This row continues that research: (1) complete the live-search-to-sample-report walkthrough that timed out previously, using a different navigation approach if the SPA-timeout issue recurs; (2) independently quantify per-state document-type coverage rather than relying on the "23+ states" company claim alone; (3) feed any genuinely new competitive finding into `docs/PLOT_TRUTH_ENGINE_SPEC.md`'s (W-90's) own source-map table, since Landeed is already listed there as the one confirmed commercial multi-state API path, UNVERIFIED on ToS. Acceptance: the live search flow is walked to an actual sample output (or the specific technical blocker is documented in enough detail for a future attempt, not just re-flagged); at least one per-state document-count data point is independently confirmed, not company-claimed. | W-90 | READY |
| W-98 | TRIGGER_DAEMON [PRODUCT: CROSS] | `scripts/FLEET_WATCH.ps1` (per-seat headless drain loop), `docs/FLEET_SEATS.json` (state file the loop reads/writes) | CRANE | **Top of CRANE's queue (2026-09-08, RULE 58).** FLEET_WATCH gains a per-seat headless drain loop: while docs/TASK_BOARD.md holds READY rows assigned to a seat and that seat is not currently rate-limited, spawn that seat's CLI process in that seat's own worktree with the standing prompt "next task" — `codex exec` for Codex seats (MASON/RIVET), `claude -p` for Claude seats (CRANE/ATLAS/SCRIBE/FERRITE/PI) — reusing W-50's existing seat-adapter distinction, not a new one. AGENTS.md and the seat's own docs/seats/<SEAT>.md load automatically from the worktree; the worktree location IS the seat's identity, no separate identity config needed. The loop re-spawns on completion, applies exponential backoff on a rate-limit hit, and waits out the recorded reset time before resuming — reusing W-50's existing `Get-CodexResetTime`/`Test-DueForRevival` mechanism directly, not reimplementing it. Every spawn and exit is logged into the same ledger-readable state file W-50 already writes (`docs/FLEET_SEATS.json` or its documented companion state file), so PI can cite real trigger evidence (spawn timestamp, exit code, dispatched row ID) in its own ledger entries rather than inferring activity from a landing alone. **IDE chat windows remain for operator override and observation only** — a human can still open a seat's IDE session to intervene or watch, but the drain loop itself must not depend on one being open. Acceptance: a full cycle where no human typed anything into any seat's chat, and PI's ledger shows a real triggered→landed→verified chain (a spawn log entry, a landing SHA, and PI's own independent verification) for at least one row, not asserted. **Citation note (2026-09-08):** a broadcast "Workflow v3" message to all seats referred to this row as "W-97" — that number is already taken by W-97 LANDEED_COMPETITIVE_FOLLOWUP (seeded moments before this row, same session); this row is, and remains, W-98, per its own original seeding entry in docs/ACTIVITY_LOG.md. Any future reference to "the harness trigger row" should cite W-98. | W-50 | READY |
| W-90 | LAND_DATA_UNIVERSE [PRODUCT: LandIntel] | new KB/adapter data (land-data source integrations, per-source ToS/access gating) — extends W-51/W-65/W-85's existing geo/parcel adapters | CRANE | **RECOVERED 2026-09-08: PI's first Execution Ledger review found `docs/PLOT_TRUTH_ENGINE_SPEC.md` (real, landed ATLAS research, commit `4e905227b`) with no board row — an unboarded research deliverable, not an omission SCRIBE introduced.** The spec is a live-verified, exhaustive land-data source map for India: ULPIN/Bhu-Aadhaar's real attribute set and national coverage status (RoR computerization 95.09% of villages, but geo-referencing — the actual ULPIN-enabling step — only 49.10%, so a "not yet geo-referenced" honest state must be surfaced per-parcel, never silently defaulted), plus a full source-by-source table (DILRMP, state RoR portals, Landeed private aggregator, Bhuvan/ISRO) each rated on coverage/accuracy/access-mode/cost and — critically — commercial-redistribution ToS status, most marked UNVERIFIED rather than assumed favorable. This row implements what the spec recommends: wiring any additional land-data sources into the existing adapter pattern (W-51/ONE-GROUND, W-65/PLOT_SEARCH, W-85/LAND_DETECT_IN), gated on real, confirmed ToS clearance per source — never integrating a source whose redistribution terms are still UNVERIFIED. Acceptance: any newly-integrated source in this row has a real, checked ToS clearance recorded (not assumed); a parcel that is RoR-complete but not yet geo-referenced surfaces that honest "not yet geo-referenced" state rather than a silent failure or a fabricated boundary. | W-51, W-65, W-85 | READY |
| W-93 | ARCHITECT_PREDESIGN_FACTS | new KB rows (real NBC Part 3 baseline + per-state Bye-Law overrides, heritage/flood/AAI-height overlay datasets) — upgrades W-24/W-29's existing SAMPLE-labeled bands | CRANE | **RECOVERED 2026-09-08: PI's first Execution Ledger review found `docs/ARCHITECT_PREDESIGN_FACTS.md` (real, landed ATLAS research, commit `363fc14f4`) with no board row.** The spec classifies every fact an architect considers before designing into Computable (Ferrum's engines derive it today — e.g. sun path, pure geometry from lat/lon), Needs KB row (a real, versioned, cited data point Ferrum doesn't have yet — e.g. real NBC Part 3 setback/FAR/coverage/height baselines plus per-state Bye-Law overrides, replacing W-24's current SAMPLE-labeled bands; ASI heritage buffer zones; CWC flood-hazard mapping), or Needs user input (no code path substitutes — e.g. vegetation/protected trees, utilities availability, a real geotechnical report beyond 2-storey presumptive SBC values). This row implements the "Needs KB row" items the spec identifies, upgrading W-24 COMPLIANCE_ENGINE's existing SAMPLE bands toward real, cited NBC/state figures — it does not fabricate Needs-user-input facts as if they were computable. Acceptance: at least the NBC Part 3 baseline (setbacks/FAR/coverage/height) is upgraded from SAMPLE to real, cited, version-tagged data per W-29's status-chip convention; the heritage-buffer-zone rule is implemented as real logic (100m prohibited/200m regulated per the Ancient Monuments Act) even where the underlying monument-location dataset itself remains a separate, smaller gap — the rule and the dataset are not conflated as one blocker. | W-24, W-29 | READY |
| W-80 | SUTRA_SINGLE_CONTACT [PRODUCT: CROSS] | `apps/web/components/Concierge.tsx`, `apps/web/lib/concierge/catalog.ts`, `apps/web/lib/concierge/intents.ts`, `apps/web/app/layout.tsx` (site-wide mount point) — removal/replacement; SUTRA mount wiring on every route — owner-agnostic | (any seat — owner-agnostic, TOP OF UI QUEUE per the operator, ahead of the W-47→...→W-69 chain) | **RULE 44 (seeded pre-generalized, per the operator's own instruction): applies to home, all ten product pages, pricing, docs, workspace, and every preview — not a single surface.** A real second assistant entry point exists on disk today — confirmed directly, not assumed: `apps/web/components/Concierge.tsx`, `apps/web/lib/concierge/catalog.ts`, and `apps/web/lib/concierge/intents.ts`, mounted site-wide via `apps/web/app/layout.tsx`. Every "Ferrum Concierge" instance, and any other assistant/chat naming or entry point beyond SUTRA, is removed from every surface. SUTRA becomes the ONE point of contact everywhere — no page offers a second, differently-named or differently-behaved assistant entry. **Amends W-59 PERSONA_ENGINE:** the persona engine now applies to every SUTRA instance site-wide, not only the cockpit's ten tabs — when a user is on a product page (not just inside the workspace cockpit), that page's SUTRA instance re-voices with the matching persona's expert title, tone, and vocabulary (per W-59's existing ten-persona table), the same one-line re-voice behavior W-59 already specifies for tab/page jumps inside the cockpit. Acceptance: a headless DOM scan across every route (home, all ten product pages, pricing, docs, workspace, previews) finds zero "concierge" strings (case-insensitive) and zero non-SUTRA assistant entry points; a SUTRA entry point is present and functional on every one of those routes; two screenshots on two different product pages show visibly contrasting persona voicing (different expert title/tone), confirming W-59's amendment actually took effect site-wide, not just in the cockpit. | W-59 | READY |
| W-81 | PRODUCT_COCKPIT_SKINS | cockpit per-tab visual-theme layer (rendering overlays/styling on the SAME underlying model, ten skin definitions) | (any seat — owner-agnostic, TOP OF QUEUE per the operator) | The cockpit re-skins per product tab with a premium domain feel over the exact same underlying model — no tab renders a different model, only a different lens on it (consistent with W-51/ONE-GROUND's existing "tabs switch the lens, never re-render the ground/model" principle). Ten skins, each paired with its matching W-59 persona voice (the skin and the persona always change together, never independently): **Design** = architect studio (trace lines, poché, door swings, material swatches, presentation-grade rendering); **Structure** = engineer (grid lines, column/beam tags, load arrows, utilization-based color coding, IS-code reference chips); **Cost** = quantity surveyor (BOQ lines highlighted against the selected element, live rates shown inline); **Procure** = vendor/LOA-focused overlay; **Build** = 4D sequence/progress overlay; **Invest** = cashflow overlay; **Market** = comparables overlay; **Community** = stakeholder-map overlay; **Transact** = legal-checklist overlay; **Land** = territory (the existing W-51/ONE-GROUND view, unchanged — this is the reference skin the other nine are built alongside, not a new one). Acceptance: switching tabs visibly changes the skin (color coding, tags, overlay elements specific to that domain) while the underlying model geometry stays identical across tabs (a headless geometry diff between two tabs' models finds zero difference); each skin's persona voice (per W-59/W-80) matches the visible skin — a screenshot pair (e.g. Structure vs. Cost) shows both the visual and voice change together. | W-51, W-59, W-80 | READY |
| W-82 | LIVE_CROSS_PRODUCT | shared project-state write path (every cockpit mutation) + live recompute wiring on every product page/preview (Structure member sizing, Cost BOQ, Procure materials, Invest cashflow) | (any seat — owner-agnostic, TOP OF QUEUE per the operator) | Every cockpit mutation — add wall/door/window, change floor count, change setback, and any other model-changing SUTRA-driven intent (per RULE 50, mutations only ever arrive via SUTRA/the intent pipeline, never a mouse handler) — writes to the shared project state, not a tab-local or page-local copy. ALL product pages and previews recompute live from that same shared state, not from a stale snapshot taken at page load: Structure's member sizing recomputes against the new geometry; Cost's BOQ recomputes the quantity/price delta; Procure's materials list recomputes; Invest's cashflow overlay recomputes. This is the connective layer W-81's skins render on top of — W-81 makes each tab look different, W-82 makes them all stay numerically correct together. Acceptance: performing a real mutation in the Design tab (e.g. add a wall) and then loading/viewing the Cost and Structure pages/previews shows the correct, updated delta on both — screenshotted as proof — with no manual refresh/recompute step required and no panel anywhere showing pre-mutation (stale) values, verified across every route this row's own acceptance names, not a spot-check of one. | W-55, W-66, W-81 | READY |
| W-47 | COCKPIT_FULLSCREEN | cockpit layout/CSS — canvas sizing, panel overlay/collapse behavior, app bar replacing the marketing header inside the workspace | MASON | **Priority insert (2026-09-05): moved to sit directly after W-26, ahead of all other UI rows, per explicit operator priority — W-47 → W-52 → W-54 → W-55 → W-58 → W-59.** The cockpit becomes a full-viewport app surface: the canvas fills the screen (100dvh minus a slim tab strip), auto-fitting the model on load and after every mutation. SUTRA chat (W-40) becomes a right overlay drawer, the extract panel becomes a bottom sheet, the territorial panel (W-33) becomes a left drawer — all three collapsible so the canvas is maximized by default. The marketing site header is replaced by a slim app bar inside the workspace specifically (the marketing header elsewhere on the site is untouched). No page scroll inside the cockpit. **Amended by W-52 (2026-09-05, RULE 44):** this row's own maximized-viewport work is the base this cockpit is built on; W-52 adds the actual OS/browser-level Fullscreen API toggle on top of it, plus the HIGH render-profile switch. **Amended by W-63 (2026-09-05, fullscreen only):** in fullscreen mode specifically, SUTRA is no longer a collapsible overlay drawer — it is a fixed, always-visible 1/3-width panel on the right; the cockpit occupies the remaining 2/3. The extract panel (bottom sheet) and territorial panel (left drawer) remain collapsible as originally specified; only SUTRA's fullscreen behavior changes. Acceptance: the canvas's bounding box occupies ≥70% of the viewport at 1366 and ≥60% at 375 in the NORMAL (non-fullscreen) state, measured by the headless check, not eyeballed; in fullscreen, the SUTRA-vs-cockpit split measures 1/3–2/3, not the ≥70% figure (that figure applies pre-fullscreen only). **Amended by W-84 (2026-09-07, top of queue):** the ≥70%/≥60% viewport-percentage acceptance above governs the COCKPIT route specifically; on the ten PRODUCT-PAGE preview embeds, W-84 replaces any percentage target with a stricter full-bleed requirement — canvas `clientWidth` must equal its containing section's `clientWidth` exactly (100%, including the currently-empty left gutter), height ≥70vh, no `max-width` wrapper on the preview section. See W-84 for the full spec; this row's own cockpit-route acceptance is unchanged. | W-40, W-33 | READY |
| W-52 | FULLSCREEN_TOGGLE | cockpit fullscreen control (browser Fullscreen API wiring) — MASON's piece; the same toggle + "Continue in workspace ⛶" button on all ten product-page previews (per RULE 44 enumeration) — MASON's piece; render-profile switch (HIGH vs. default, per RULE 41's degradation-profile mechanism) — MASON's piece | MASON | **Priority insert (2026-09-05): moved to sit directly after W-47, ahead of all other UI rows, per explicit operator priority — W-47 → W-52 → W-54 → W-55 → W-58 → W-59.** A YouTube-style fullscreen button on the cockpit AND on every one of the ten product-page previews (generalized per RULE 44 from a single "cockpit" ask, since the operator's own instruction named "all ten" explicitly). Uses the browser Fullscreen API on the cockpit container. In fullscreen: the slim 10-tab strip stays on top; SUTRA/extract/tools panels become overlays (reusing W-47's overlay/drawer mechanism, not a new panel system); the render profile switches to HIGH — full shadows + reflections + pixelRatio 2 + all viewports (the opposite end of RULE 41(2)'s WebGL degradation-profile spectrum, using the same capability-detection mechanism). Esc or the same toggle button exits fullscreen. **Acceptance amended 2026-09-05 (RULE 44, applies to all ten product-page previews + cockpit route):** on each product-page preview, a prominent "Continue in workspace ⛶" button renders at the RIGHT END of the view-tab strip (the 3D space / Plan / Front / Side row) — visible without scroll, above the canvas, never buried at the card bottom. One click carries current state straight into FULLSCREEN workspace mode. The same slot on the cockpit route itself holds the fullscreen toggle (no separate/duplicate control). W-54's chip consolidation applies here too, so the canvas stays visually clean behind this button. Mobile: Fullscreen API where supported; a 100dvh immersive fallback (chrome hidden) where it isn't, never a silent no-op. Acceptance: fullscreen screenshots at 1366 and 375 show the tab strip plus a visibly higher-detail canvas (HIGH profile); the "Continue in workspace ⛶" button is visible in the tab row at both viewports without scrolling; the fullscreen state after one click is shown; fps is measured and reported in both the default and HIGH profiles per RULE 41(3); no page scroll occurs inside fullscreen. **Acceptance amended 2026-09-05 (RULE 44 — every surface showing Save shows Open beside it):** the "Continue in workspace ⛶" button above is the same control W-33 now calls "Open in workspace ⛶" wherever a Save action exists (record card, extract panel, artifacts, previews) — one control, two row-references, not a duplicate mechanism. One click both navigates with preloaded context (W-33) AND enters this row's fullscreen mode, in the same click. | W-47, W-41 | **DONE** — `2deb09dd` ("[land:mason/w52-fullscreen-controller]"), touching `FullscreenController.tsx` (new), `WorkspaceCockpit.tsx`, `CanvasSlot.tsx`, `Space3D.tsx`, `ProductCockpitPreview.tsx`, `cockpit/page.tsx` — matches this row's stated envelope (2026-09-06 SCRIBE reconciliation). RULE 25 deployed-edge live verification not independently performed by SCRIBE; this DONE marks landing only. |
| W-54 | CHIP_CONSOLIDATION + FULLSCREEN_VISIBLE | provenance-chip rendering (cockpit + all ten product-page previews) — MASON's piece; fullscreen button visibility/z-index fix on cockpit + previews — MASON's piece | MASON | **Top UI priority, seeded pre-generalized per RULE 44 (operator named "cockpit + all previews" directly) — sits directly after W-52, ahead of all other UI rows: W-47 → W-52 → W-54 → W-55 → W-58 → W-59.** The three stacked provenance boxes (attribution chip, imagery-date chip, and any other status/source chip currently rendered as separate stacked boxes — per W-51/ONE-GROUND's attribution+date chips and W-29/W-43's status-chip vocabulary) collapse into ONE slim status bar: bottom-left, one line, showing all the same information compactly rather than as three stacked boxes. Applies everywhere those chips currently render: the cockpit (all ten tabs) and every product-page preview. The YouTube-style fullscreen button (W-47/W-52) must render VISIBLY on the cockpit and all previews — this row is also the completion/visibility fix for W-47/W-52's button if it is not actually rendering or is obscured (z-index/layout conflict with the consolidated status bar is the likely cause, since both occupy screen-corner space; this row's implementer resolves that conflict as part of the same pass, not as a separate row). Acceptance: screenshots at 1366+375 on the cockpit and at least one product-page preview show exactly one slim status-bar line (not three stacked boxes) and a visibly rendered, clickable fullscreen button; the button is verified clickable (not just visually present) on the deployed edge. | W-47, W-52, W-51 | **DONE** — `0d2d850d` ("[land:mason/w54-chip-consolidation]"), touching `Space3D.tsx` — matches this row's stated envelope (2026-09-06 SCRIBE reconciliation). RULE 25 deployed-edge live verification not independently performed by SCRIBE; this DONE marks landing only. |
| W-55 | PLAN_QUALITY | dimensional-standards module wiring into PLAN_GEN + PLAN_AUDIT deterministic test — CRANE's piece (dimensional data, from W-29's tables); plan-generation engine changes to obey the wired standards — MASON's piece | MASON (engine) + CRANE (dimensional data) | **Top priority with W-47/W-52/W-54 (2026-09-05) — sits directly after W-54, ahead of all other rows below this priority block.** The dimensional-standards module (W-29's tables) is wired INTO PLAN_GEN, not read informationally alongside it — the generator must actually obey every constraint below, not just display them: wall poché (exterior 230mm / interior 115mm); every room reachable by exactly one door from circulation, never room-through-room; door leaves 900mm or 750mm with swing arcs drawn; stair core sized per the KB stair module (width/tread/riser ranges); room dimension ranges plus aspect ratio 1:1–1:2 per program type (living/bed/kitchen/bath minima sourced from the standards module, each provenance-cited per W-29's clause-ID/status-chip convention, never a hardcoded number with no traceable source); adjacency rules enforced (entry→living, kitchen→dining, wet cluster→stair/shaft); daylight rule: habitable rooms sit on external walls with window lines, wet cores sit internal with a shaft. Drawing conventions: room labels, dual-unit areas (per RULE 30), north arrow, scale bar. **DETERMINISTIC PLAN_AUDIT test** (new, this row's own gate) asserts every one of: dimension/aspect ranges, adjacency rules, door count per room, and circulation reachability (a graph-reachability check from every room to circulation, not a visual spot-check) — an audit fail blocks this row's landing the same way a type-check failure would, no partial-pass. **RULE 44 generalization (seeded pre-generalized, per the operator's own instruction):** applies to the Plan view, DXF export (W-06), and all previews — a plan that violates the audit must not reach any of those three surfaces. Acceptance: PLAN_AUDIT runs in CI and a deliberately non-compliant test plan (bad aspect ratio, room-through-room, missing door) actually fails it — a real negative test, not just the audit existing; a compliant generated plan passes with all provenance citations present; the Plan view, DXF export, and preview surfaces all reflect the same audited plan, not divergent copies. | W-29, W-24 | READY |
| W-58 | ARCHITECT_INTAKE_TREE | new versioned decision-tree data file (branching node schema) — CRANE's piece (content authoring); tree-driven UI renderer (one question at a time, chips, back/progress/skip, voice-optional) + END STATE candidate-generation wiring — MASON's piece | CRANE (content) + MASON (renderer) | **Top priority (2026-09-05) — sits directly after W-55, ahead of all other rows below this priority block. Supersedes W-28 (amended above).** A declarative, versioned branching decision tree encodes the FULL professional architect intake dialogue — every question a practicing architect asks to start design, including interior design; not a subset, not a simplified version. Node coverage, all required: (1) site & land — dimensions, orientation, road side, slope, existing structures/trees, climate; (2) family & lifestyle — members, ages, elders, WFH, guests, pets, vehicles, hobbies; (3) program — full rooms list including pooja/study/store/utility/balcony/terrace; (4) budget & grade; (5) style & massing — floors, roof, courtyard; (6) INTERIOR branch — kitchen layout (L/U/parallel/island), flooring, wardrobes, ceiling, lighting mood, palette, modular furniture; (7) culture & comfort — Vaastu preferences, daylight, ventilation, solar, rainwater; (8) future — expansion, rental, ageing-in-place. UX contract: ONE question at a time, big option chips (not free-text-first), every chosen option branches to the next relevant node only — irrelevant branches are skipped, never shown and skipped-through; back, progress indicator, and skip controls always present; voice is optional, never required; zero typing required to complete the full tree. Each node carries a one-line "why an architect asks this" explanation, so the tree teaches while it asks. **END STATE:** the engine generates 2-3 final house candidates from the completed tree ("AI proposes / Ferrum proves" — each candidate scored, scores shown, not just presented as equally valid); the user picks one final house, and the workspace cockpit opens pre-seeded with that exact choice, not a generic starting state. **RULE 44 (seeded pre-generalized, per the operator's own instruction):** this tree is THE entry point on all surfaces — product pages, the SUTRA panel, and onboarding — not a single-page feature; any surface that starts a new design without routing through this tree is a gap, not an accepted alternate path. Acceptance: completing the full tree end-to-end with zero typed input (chips + skip/back only) reaches the 2-3 scored candidates; each node's branching correctly skips every irrelevant subsequent node for a given path (verified for at least 2 distinct paths); the "why an architect asks this" line is present on every node, not just a sample; product pages, SUTRA, and onboarding all launch the same tree instance, not divergent copies. **Amended by W-63 (2026-09-05):** the intake questionnaire renders INSIDE the SUTRA panel as its fallback mode, not as a separate full-page flow — a "Can't describe it? Choose instead" toggle inside SUTRA surfaces the branching chip tree from this row; the user's chip answers flow into the same prompt pipeline SUTRA's text/voice input uses, not a separate code path. This does not replace SUTRA's primary text/voice mode — it is the explicit fallback for a user who prefers guided chips over describing in words. Acceptance (added): the "Can't describe it? Choose instead" toggle is visible and functional inside the SUTRA panel, and completing the chip tree from inside SUTRA produces the same 2-3 scored candidates as completing it via free text/voice. | W-24, W-28, W-29 | READY |
| W-59 | PERSONA_ENGINE | per-product persona config module (id, expert title, tone descriptors, vocabulary set, bound KB modules, greeting line, default intake branch) — CRANE's piece; SUTRA wiring to load/re-voice on tab or page jump + header display — MASON's piece | CRANE (configs) + MASON (wiring) | **Top priority (2026-09-05), pulls after the W-47→W-52→W-54→W-55→W-58 chain.** Ten per-product persona configs, one per cockpit tab/product, each binding: `{id, expert title, tone descriptors, vocabulary set (per W-30's ontology), bound KB modules (W-29/W-41/W-43 as applicable to that persona), greeting line, default intake branch (into W-58's tree)}`. The ten personas, as given: Land = Land Due-Diligence Consultant (title/ULPIN/encumbrance/zoning); Design = Chartered Architect; Structure = Structural Engineer (IS 456/1893); Cost = Quantity Surveyor (BOQ/DSR/GST); Market = Market Analyst (comparables/yields); Procure = Procurement & Contracts Manager (tenders/LOA/contract clauses); Invest = Investment Analyst (IRR/NPV/sensitivity); Build = Construction Project Manager (sequence/logistics/QA-QC); Community = Approvals & Community Liaison (NOCs/society); Transact = Property Legal Advisor (sale deed/stamp duty/registration). On every tab/page jump, SUTRA re-voices in exactly one line (e.g. "Structure now — I'll speak limit states, loads and spans with you") — a real voice/tone change, not just a label swap. Every SUTRA answer draws ONLY from that persona's bound KB modules and is cited (per W-29/W-41's clause-ID convention) — a persona never answers from a module it isn't bound to, and never presents an unbound-module fact as its own expertise. The SUTRA header displays the current expert title plus a knowledge-depth chip sourced from W-41's coverage manifest — honesty (VERIFIED-SAMPLE/INDICATIVE/roadmap) persists per-persona, not just globally. **RULE 44 (seeded pre-generalized):** personas apply everywhere SUTRA appears — cockpit tabs, product-page previews, the SUTRA panel itself, and W-58's intake branches (the tree's own questions are voiced in the relevant persona, not a neutral narrator). Acceptance: switching tabs/pages triggers the one-line re-voice every time, verified across at least 3 of the ten personas; a domain answer in each tested persona cites only that persona's bound modules (spot-checked against the module list, not just plausible-sounding); the header's expert title + knowledge-depth chip update correctly on every switch. **Amended by W-80 (2026-09-06, RULE 44):** persona re-voicing now applies to every SUTRA instance site-wide, not only the cockpit's ten tabs — a product page's own SUTRA entry point re-voices with the matching persona when the user is on that page, using the same one-line re-voice mechanism this row already specifies. | W-29, W-30, W-41, W-43, W-58 | **CRANE's config piece DONE (2026-09-07):** `apps/web/lib/sutra/personas.ts` - all ten persona configs (id/expertTitle/toneDescriptors/vocabularySet/boundKbModules/greetingLine/defaultIntakeBranch), verbatim expert titles per the operator's list. `vocabularySet` binds only to real W-30 `CanonicalTerm`s (tested); `boundKbModules` binds only to real `KbDomain` values from W-29's schema (tested), not invented module names. `defaultIntakeBranch` is a forward-reference string into W-58's tree (not yet built) - a placeholder node id, not a functional link yet; flagged rather than blocking on W-58. **MASON's wiring piece (load/re-voice on tab/page jump + header display) not started** - this row is not DONE until that lands and the acceptance's live tab-switch/re-voice verification passes. |
| W-60 | KB_MAX_DEPTH | extends W-29/W-41/W-43's existing data modules with a continuous adapter pipeline (RATE_ENGINE/W-46 pattern) + a per-module REFINE slot + an honest per-domain coverage-depth manifest — CRANE's extraction piece; per-domain depth-% audit against the manifest — ATLAS's piece | CRANE (extraction) + ATLAS (audit) | **Amends W-29 KNOWLEDGE_BASE, W-41 KB_EXHAUSTIVE, and W-43 MATERIALS_CATALOG's scope — this row does not replace them, it deepens their existing data modules toward the last available public data on the planet, domain by domain.** Domains, each pulling every available public data source rather than a sample: codes (NBC 2016 + amendments, IS 456/875/1893, SP 7, all four existing city DCR samples extended to full text where public); government rate lists (CPWD + state PWD, refreshed quarterly per the adapter pipeline below, not a one-time snapshot); manufacturer catalogs/technical data sheets (full brand depth per W-43's existing brand list, not a curated subset); market indices; stamp-duty schedules; tender/contract standards (IS/CPWD contract documents); interior/ergonomic standards (Neufert-class dimensional data, feeding W-58's interior branch); material science datasheets. **Continuous adapter pipeline (reuses W-46 RATE_ENGINE's pattern, not a new mechanism):** each domain's public sources stream in through a pluggable adapter, every fact carrying a fetch-date and the existing status-chip vocabulary for its data class (W-29/W-41's VERIFIED-SAMPLE/INDICATIVE, W-43's SAMPLE/vendor-API, W-46's VERIFIED-PUBLIC/REFERENCE-ONLY/CONCEPT/LIVE, as applicable per module — no forced single vocabulary across modules). **Operator refinement queue:** every module carries a REFINE slot — a standing input point where the operator's own corrections or additions override whatever the adapter pipeline populated; the operator's domain knowledge is the final authority over any public-source sample, and a REFINE entry is never silently superseded by a later adapter run. **Coverage manifest (extends W-41's existing manifest, not a second one):** shows a per-domain depth percentage honestly — a domain with thin public-source coverage shows a low percentage, never inflated or silently omitted. **RULE 44 (seeded pre-generalized):** the same continuous-pipeline + REFINE-slot + honest-depth-% pattern applies uniformly across W-29/W-41/W-43's existing modules and every domain listed above, not to one module while others stay static. Acceptance: a per-domain item-count table plus the actual source list is produced and matches the coverage manifest's stated percentages (audited by ATLAS, not self-reported by CRANE); zero facts in the deepened modules are sourced from memory/assumption — every fact traces to a cited public source or an operator REFINE entry, verified by spot-check across at least 3 domains. | W-29, W-41, W-43, W-46 | READY |
| W-61 | HOME_AFFORDANCE | workspace app bar component (normal cockpit, fullscreen mode, and all product-page previews) | MASON | On the workspace app bar — in its normal state, in W-52's fullscreen mode, and on every product-page preview's equivalent bar — the "Ferrum" brand mark becomes a clickable home link, AND a separate, explicit Home button renders beside the existing Territory/Extract/SUTRA controls. Either affordance gets the user back to the main site in one tap, from any app surface including fullscreen — the slim app bar (per W-47) keeps this control visible even in fullscreen, it is never dropped to save space. **RULE 44 (seeded pre-generalized, per the operator's own instruction):** applies uniformly to normal cockpit, fullscreen, and all previews — not just the cockpit's default state. Acceptance: screenshots of the normal workspace app bar and the fullscreen app bar both show the clickable brand mark and the explicit Home button; a click-through on each actually reaches the main site (not a dead link or a same-page no-op), verified on the deployed edge. | W-47, W-52 | **DONE** — `3653c8bb` ("[land:mason/w61-home-affordance]"), touching `cockpit/page.tsx`, `WorkspaceCockpit.tsx` — matches this row's stated envelope (2026-09-06 SCRIBE reconciliation). RULE 25 deployed-edge live verification not independently performed by SCRIBE; this DONE marks landing only. |
| W-63 | FULLSCREEN_THIRDS | fullscreen layout region law (SUTRA panel width + cockpit width in W-52's fullscreen mode) — amends W-47/W-40; SUTRA-panel fallback questionnaire toggle + wiring into W-58's tree — amends W-58 | MASON | In W-52's fullscreen mode: the SUTRA panel occupies exactly 1/3 of the viewport width on the right; the cockpit (canvas + tab strip) occupies the remaining 2/3 — a fixed, measured split, not an approximate one. Amends W-47's fullscreen region law (SUTRA is no longer a collapsible overlay drawer in fullscreen — it is this fixed 1/3-width panel) and W-40's region law (this 1/3–2/3 fullscreen ratio sits alongside, not instead of, W-40's normal-mode "SUTRA = right side, default open" rule). **Amends W-58 ARCHITECT_INTAKE_TREE:** the intake questionnaire renders INSIDE the SUTRA panel as its fallback mode — a "Can't describe it? Choose instead" toggle inside SUTRA surfaces W-58's branching chip tree; chip answers flow into the same prompt pipeline as SUTRA's text/voice input, not a separate code path. **RULE 44 applies (seeded pre-generalized):** the 1/3–2/3 split and the in-SUTRA questionnaire fallback both apply wherever fullscreen/SUTRA exist together, not to one route only. **RULE 50 cross-check (adopted same pass):** the cockpit canvas inside this 2/3 region carries no mouse-mutation handlers — view gestures only (orbit/pan/zoom), consistent with RULE 50 rather than a separate concern this row introduces. Acceptance: a fullscreen screenshot with the SUTRA-vs-cockpit split measured at 1/3–2/3 (not eyeballed); the questionnaire is visible and functional inside the SUTRA panel; a DOM/event-listener scan of the canvas confirms no mouse-mutation handlers are attached (view gestures only). | W-40, W-47, W-52, W-58 | READY |
| W-64 | BOOTSTRAP_LAUNCH | not on disk — see flag below | (unassigned pending scope) | **Flagged, not fabricated (RULE 27/39/43):** this row's own original title and full scope were never seeded on this board or found anywhere on disk (`docs/TASK_BOARD.md`, `docs/WAVE_QUEUE.md` both checked, no match) — only an amendment instruction citing "W-64 BOOTSTRAP_LAUNCH" arrived, with no prior turn establishing this row's content. Per RULE 39 (row IDs are annotations, not authority) and RULE 43 (citation-on-main), the amendment's own text is unambiguous and self-contained, so it is recorded here in full rather than discarded — but the row's other fields (envelope, eligible seat, full acceptance) are NOT invented and stay open pending the actual BOOTSTRAP_LAUNCH scope. **Amendment text, as given verbatim (2026-09-05):** "bootstrap = zero investor dependency, NOT reduced features; the full roadmap ships on free tiers; premium data sources are upgrades, not gates." Whoever authored or holds the original W-64 BOOTSTRAP_LAUNCH scope (conductor/operator) should supply it so this row can be completed with a real envelope, eligible seat, and acceptance criteria — until then this row is not pull-eligible under RULE 35. **Live-URL single source of truth recorded here (2026-09-05, per this row per the operator's own instruction):** the current live URL is `https://ferrumos-preview.shariefsatyala.workers.dev`, verified directly (HTTP 200) after the worker rename in `crane/rename-worker-privacy` (`wrangler.jsonc`'s `name` field, `ferrum-os` → `ferrumos-preview`); the prior `https://ferrum-os.shariefsatyala.workers.dev` now returns 404, also verified directly. The authoritative record is `docs/FLEET_SEATS.json`'s `deployment` block — every seat and ATLAS's battery read the URL from there, not from a locally remembered or hardcoded value; `docs/MEETING_TECH_REPORT.md` §2 mirrors the same value and cites that block as its source. **Go-live currency update (2026-09-06, SCRIBE stewardship pass):** consolidated a second, independently-created live-URL file (`docs/FLEET_CONFIG.json`, used by `scripts/get-live-base-url.mjs`) into this same `docs/FLEET_SEATS.json` `deployment` block, retiring the duplicate rather than leaving two sources of truth. Found and flagged a real go-live blocker in the same pass: the Android mobile shell (`apps/mobile/**`) still hardcodes the dead pre-rename URL in its Capacitor config and network-security allowlist, meaning the shipped app currently cannot reach the live edge at all — tracked as W-83 MOBILE_SHELL_DEAD_URL, not fixed silently here (out of SCRIBE's docs-only scope; RIVET's exclusive path). | — | STUCK (BOOTSTRAP_LAUNCH scope only) — original row scope not found on disk, amendment text preserved verbatim above; the live-URL recording above is independently complete regardless of that gap. |
| W-65 | PLOT_SEARCH | LandIntel lookup UI + Land tab + Territory panel (per RULE 44) — MASON's UI piece; geocoder adapter policy (Nominatim usage terms, rate limiting, self-hosting roadmap) — CRANE's piece | MASON (UI) + CRANE (policy) | The LandIntel lookup, the cockpit Land tab, and the Territory panel (per RULE 44, generalized across all three since the operator named them together) gain three input modes, all resolving to the same site-context pipeline: (1) **PLACE search** — a Google-style typeahead using the Nominatim geocoder, debounced and cached client-side, capped at 1 request/second per Nominatim's usage policy, carrying an attribution chip (OSM/Nominatim) at all times; self-hosting Nominatim for commercial scale is recorded as a roadmap item, not built now. (2) **COORDINATES** — direct decimal lat/lon entry, plus DMS (degrees/minutes/seconds) parsing into the same decimal form, both validated (range-checked, not accepted blindly) before use. (3) **ULPIN** — the existing lookup, kept unchanged and seeded now; official ULPIN registry integration stays a later, separate step, not blocked by this row. On any successful resolve (place, coordinates, or ULPIN): the site context's coordinates are set, OSM tiles and building footprints re-drape at the resolved bounding box using W-51/ONE-GROUND's existing source adapters (no new rendering path), and a provenance chip reads "user-provided location · source · date" for place/coordinate resolves — the parcel boundary itself is marked indicative until an actual survey exists, never implied precise from a geocoded point alone. Acceptance: searching "Indiranagar, Bengaluru" recenters the map and ground at that place, with the attribution and provenance chips both visible; entering coordinates directly produces the same recenter behavior; the existing ULPIN chip/flow is unchanged and still functions exactly as before this row. **Amended by W-85 (2026-09-07, top of queue):** this row's three input modes (PLACE, COORDINATES, ULPIN) are the first three of six W-85 defines — W-85 adds MAP PIN DROP, USE-MY-LOCATION (Geolocation API), and a roadmap-labeled survey/khasra/plot-number mode, and additionally requires a disclosed default map center (never a stray/unexplained pin). See W-85 for the complete six-mode spec and the SUTRA/UI cleanup it bundles in the same pass. | W-51 | READY |
| W-66 | MINIMAL_ELEMENT_SET [PRODUCT: CROSS] | plan-generation engine element pipeline (columns/beams/slabs/walls/stairs) + BOQ line mapping | MASON | The model builds the bare-minimum set of elements a real building actually needs, not a stylized shell: columns (a grid derived from spans, per W-55's dimensional-standards wiring), beams running along the grid lines, slabs/ceilings per floor, RCC and masonry walls placed on the plan's actual partitions (per W-55's PLAN_AUDIT-compliant layout), and stairs/steps generated from the KB stair module (width/tread/riser ranges, same source W-55 already wires in). Every element maps to a real BOQ line — concrete volume (m³) for columns/beams/slabs, steel weight (kg) for reinforcement, masonry area (m²) for walls — so toggling or resizing any element updates its BOQ line live, not on a separate recompute step. Acceptance: element visibility toggles are present and functional in the 3D view (columns/beams/slabs/walls/stairs each independently toggleable); changing an element (e.g. wall length, column count) produces a visible, correct BOQ line delta in the same session, verified on the deployed edge. | W-55, W-44 | READY |
| W-67 | PRESET_LIBRARY [PRODUCT: CROSS] | combinatorial preset-generation script + audit-score/stats storage — MASON's generation piece, CRANE's seeding piece; library browser UI (thumbnail cards + stats) + baseline-swap-into-workspace flow — MASON's piece | MASON (gen + UI) + CRANE (seed) | A large library of preset baseline plans, generated combinatorially from the plan-generation engine (W-55/W-66) rather than hand-authored one at a time: plot classes (30×40, 20×30, 40×60, and other common sizes, as given) crossed with uses, floor counts, and massing styles. Every generated preset must pass W-55's PLAN_AUDIT before being stored — a failing combination is not added to the library, never silently included with a lower score. Each stored preset carries its audit score and key stats (area, floor count, room count, BOQ summary). The library browser shows one card per preset: a plan thumbnail plus its key stats, browsable/filterable. **Flow (RULE 50-compliant):** a user picks a baseline preset card, it swaps into the workspace while KEEPING the current parcel/site context (per W-33/W-51 — the preset supplies the building, not the site), and the user then edits the floor plan exclusively via SUTRA intents and/or W-58's questionnaire fallback (per W-63) — the mouse stays view-only throughout this flow, per RULE 50 (a preset card click is a selection action, not a mutation-by-mouse exception; W-53's slider+paired-input carve-out is a separate, distinct mechanism this flow does not need). Acceptance: at least 24 seeded presets are stored and browsable in the library, each with a real PLAN_AUDIT pass and real stats (not placeholder numbers); picking a preset swaps it into the workspace with the parcel context intact, verified on the deployed edge; a transcript proof shows a subsequent SUTRA-driven edit (e.g. "add a floor") applied to the swapped-in preset, confirming the edit path works post-swap. | W-55, W-66, W-33, W-58, W-63 | READY |
| W-68 | TECH_SCOUT | none — audit/research only, no source envelope | ATLAS | **Interpreted from seat+title only (RULE 27/39) — the instruction gave no further scope, unlike every other row seeded this session; flagged rather than treated as fully specified.** By analogy to the existing RULE 26/docs/SKILL_SCOUT.md pattern (which scouts AI agent skills, rotating starting with SCRIBE) and this session's own W-35a IMG2THREEJS_DUE_DILIGENCE precedent (a seat scouting one specific external technology with a real verdict), this row is provisionally scoped as: ATLAS scouts external technologies/libraries relevant to Ferrum's current engineering surface (3D/BIM rendering, geocoding, plan-generation/CAD tooling, PDF/document extraction, and similar areas already touched by W-20/W-35a/W-51/W-65/W-66) and reports findings with a real verdict per finding — ADOPT-TRIAL (requires an docs/APPROVAL_QUEUE.md row before anything installs), WATCH, or SKIP — never a bare "found this, seems useful" note. Distinct from RULE 26's own skill-scouting (which covers AI agent skills/capabilities, not engineering technology) — the two scouting duties are not the same row. **This scope is ATLAS's own to confirm or correct on first pull** — if the operator meant something narrower or different by "TECH_SCOUT," this row should be amended once that's clarified, not treated as settled. Acceptance (provisional, pending confirmation): at least one real technology is scouted with a genuine adopt/watch/skip verdict and stated pain-mapping (the specific problem it would solve), logged where ATLAS judges most appropriate (a new docs/TECH_SCOUT.md, mirroring SKILL_SCOUT.md's schema, is the default absent a better-fitting existing doc). | — | **DONE — `docs/TECH_SCOUT.md` landed (`a5ac86ed`).** 13 targets researched, each independently verified live via `gh api`/actual LICENSE-file reads (not recalled): use-as-is (2), adapt (4), concept-only (4), reject (2 — `braveopotatato` doesn't exist, `math-curve-loaders` has no license/no feature fit), unverifiable-as-named (1). This row's provisional scope is confirmed correct by the fact its own output seeded the entire W-70..W-79g integrations board. |
| W-69 | UX_OVERHAUL [PRODUCT: CROSS] | marketing site hero + journey strip + all ten product cards (home page); per-product-page hero/preview treatment; shared design-system tokens/CSS; pricing + docs pages | RIVET | **RULE 44 (seeded pre-generalized, per the operator's own instruction): applies to home, all product pages, and pricing/docs — not the homepage alone.** (1) **HERO:** the static system-map card is replaced with a LIVE embedded cockpit teaser — a mini 3D canvas showing a slow orbit of the massing on the real ground (per W-51/ONE-GROUND, not a stock render) — the product demonstrates itself rather than being described. (2) **JOURNEY STRIP:** Land→Design→Build→Invest becomes an interactive animated stepper; hovering a stage previews it, clicking scrolls to the matching product section. (3) **PRODUCT CARDS:** all ten cards get an icon, a one-line value prop, and a LIVE micro-preview specific to that product (a ticking rate for Cost/RATE_ENGINE, a pulsing map pin for Land, a counting GFA figure for Design, a rotating thumbnail for Structure, etc. — one honest live signal per card, not a generic animation reused ten times), plus a hover lift/glow and an "Explore →" affordance; cards reveal on scroll with a staggered timing, not all at once. (4) **DESIGN SYSTEM:** keeps the existing navy/orange token palette — this is a polish pass, not a rebrand; adds soft-shadow/glass accents, stronger type-scale contrast, a sliding tab-underline indicator, and button press feedback; `prefers-reduced-motion` is respected everywhere motion is added, per accessibility, not an afterthought. (5) **Sources, all free/MIT, no new paid dependency:** shadcn/ui components, magicui/aceternity-style CSS animation patterns (referenced/adapted, not a paid library), framer-motion for JS-driven interaction where CSS alone can't do it, lucide icons. Animation is CSS-first specifically to protect RULE 41's bundle budget (cockpit route ≤600KB gz, other routes ≤350KB gz) — framer-motion is the fallback for what CSS genuinely can't express, not the default. Acceptance: before/after screenshots of the home page and at least one product page at 1366+375; recorded evidence of the hover/animation behaviors actually firing (not just present in code); RULE 41's budget and fps floor both still pass post-change — a regression here blocks landing the same as any other RULE 41 gate. | W-34, W-51 | READY |
| W-70 | INTEGRATE_WHISPER [PRODUCT: CROSS] | new Worker route accepting an audio blob for transcription; SUTRA voice-input fallback wiring | (any seat — owner-agnostic, first to pull) | Per `docs/TECH_SCOUT.md` #1 (`openai/whisper`, MIT, verified via live `gh api` call): mode **Adapt**. Fixes a real, existing gap — `SutraPanel.tsx` depends on `SpeechRecognition`/`webkitSpeechRecognition`, unreliable/absent on some Safari versions, currently showing an honest "Voice input is unavailable here" fallback there. Adds a server-side transcription path (self-run Whisper model or a hosted Whisper API — the specific hosting decision is this row's own to make and record, not pre-decided) independent of any browser's native speech API; SUTRA falls back to this path when `webkitSpeechRecognition` is absent, never silently staying broken. Acceptance: a voice input on a browser without native `SpeechRecognition` support successfully transcribes via this new path, verified on the deployed edge; the existing native-speech path is unaffected where it already works. | W-27 | READY |
| W-71 | INTEGRATE_CALDIY [PRODUCT: CROSS] | booking-CTA component + hosted scheduling-widget embed | (any seat — owner-agnostic, first to pull) | Per `docs/TECH_SCOUT.md` #12 (`calcom/cal.diy`, MIT, verified): mode **Use-as-is** — embed/self-host the scheduling widget, do not fork its codebase. A real "Book a consult" CTA (no such flow currently exists on disk) using cal.com's hosted free tier for now; self-hosting is a recorded roadmap step, not built in this pass. Acceptance: the CTA is live on at least one marketing surface and a real booking flow completes end-to-end against the hosted widget, verified on the deployed edge. | — | READY |
| W-72 | INTEGRATE_AGUI [PRODUCT: CROSS] | SUTRA↔workspace event-protocol refactor (`SutraPanel.tsx`'s `onSubmit` callback, `cockpit/page.tsx`'s `handleCommand`) | (any seat — owner-agnostic, first to pull) | Per `docs/TECH_SCOUT.md` #9 (`ag-ui-protocol/ag-ui`, MIT, verified): mode **Adapt**. Replaces the current bespoke `onSubmit(text: string)` callback with AG-UI's structured event-schema conventions (text message, tool call, state delta) as SUTRA's guided-tree/command-bar surface (W-27/W-58) grows past what one string callback can carry. Acceptance: at least one real interaction (e.g. a W-58 guided-tree chip selection) round-trips through the new event schema end-to-end, verified on the deployed edge; the existing text/voice command path is unaffected. | W-27, W-58 | READY |
| W-73 | INTEGRATE_DIAGRAMGEN [PRODUCT: CROSS] | new diagram-generation module (HTML+SVG, no heavy charting library) | (any seat — owner-agnostic, first to pull) | Per `docs/TECH_SCOUT.md` #6 (`cathrynlavery/diagram-design`, MIT, verified) and #7 (`tt-a1i/archify`, MIT, redundant with #6 as a second data point on the same pattern, not a separate integration): mode **Adapt**. Its self-contained HTML+SVG diagram-generation approach fits this repo's static-export site. First real use: an auto-generated massing/compliance-chain diagram for a project (an "explain this building" feature), which also directly serves this fleet's own `docs/MEETING_TECH_REPORT.md`'s diagramming needs (per its own W-68 finding). Acceptance: at least one real, data-driven diagram renders from actual project/compliance data (not a static placeholder image), verified on the deployed edge. | — | **DONE** — `d73f881a` ("[land:crane/w73-diagram-gen]"), adding `lib/diagramGen/complianceChain.ts` + `svgFlowDiagram.ts` (+ tests) and wiring into `WorkspaceCockpit.tsx` — matches this row's stated envelope (2026-09-06 SCRIBE reconciliation). RULE 25 deployed-edge live verification not independently performed by SCRIBE; this DONE marks landing only. |
| W-74 | INTEGRATE_N8N [PRODUCT: CROSS] | self-hosted n8n deployment + Ferrum-specific workflow nodes for KB/rate-refresh and fleet job automation | (any seat — owner-agnostic, first to pull) | Per `docs/TECH_SCOUT.md` #4 (`n8n-io/n8n`, "Sustainable Use License," GitHub reports `NOASSERTION`, verified): mode **Adapt** — self-host n8n as the job-runner/orchestrator, wire Ferrum-specific workflow nodes; do not fork n8n's own code. **Prerequisite sub-task, same row, blocks all other work here:** confirm against n8n's actual license text (not assumed from the GitHub license badge alone) that self-hosting for internal automation — KB/rate-refresh jobs (W-60), the W-50 fleet harness's own scheduled tasks — without reselling n8n itself or building a competing commercial SaaS on top of it, is within the Sustainable Use License's terms; record the confirmation (or the specific restriction found) on this row before any n8n deployment work proceeds. Acceptance: the license confirmation sub-task's finding is recorded on this row first; only then, at least one real Ferrum-specific workflow (e.g. a W-60 KB-refresh job) runs successfully end-to-end on the self-hosted instance. | W-50, W-60 | **License confirmation DONE (2026-09-06):** fetched the real Sustainable Use License text directly from n8n-io/n8n's repo (LICENSE.md, not the GitHub badge inference) - "You may use or modify the software only for your own internal business purposes..."; "...distribute...only...free of charge for non-commercial purposes." Self-hosting for internal automation (KB/rate-refresh, fleet deploy alerts) is within these terms - no resale, no third-party exposure, no competing SaaS. **Workflow definitions authored (automation/n8n-workflows/): kb-rate-refresh.json, deploy-alert-fanout.json** - schema-correct against n8n's documented export format, NOT yet import-verified (no running n8n instance exists yet - self-hosting itself is deferred per the operator's own sequencing, "author now, self-host later"). Remaining before DONE: stand up the self-hosted instance and confirm at least one workflow runs end-to-end for real. |
| W-75 | INTEGRATE_PLAUSIBLE | self-hosted analytics deployment (separate service) + tracking snippet on marketing site + workspace | (any seat — owner-agnostic, first to pull) | Per `docs/TECH_SCOUT.md` #2 (`plausible/analytics`, AGPL-3.0, verified): mode **Use-as-is**, deployed as a separate self-hosted service, not embedded into this repo's own codebase — AGPL-3.0 governs modifications to Plausible's own source, not the site being measured, so using it unmodified carries no copyleft obligation on Ferrum OS's code. Replaces having no analytics at all (none currently found on disk). Cookie-free, privacy-first, per the existing honesty/no-dark-patterns standard this fleet already holds itself to. Acceptance: the self-hosted instance is live and the tracking snippet is confirmed recording real pageviews on the deployed marketing site, verified directly (not assumed from deployment success alone). | — | STUCK — public Plausible host/analytics hostname absent; single operator question posted once in `docs/OPERATOR_INBOX.md` (2026-09-06). |
| W-76 | CONCEPT_SPEC_PENPOT | written concept spec + a minimal viable adaptation of selection/handle/snap-to-grid interaction patterns for DesignStudio's Plan/Front/Side tabs | (any seat — owner-agnostic, first to pull) | Per `docs/TECH_SCOUT.md` #3 (`penpot/penpot`, MPL-2.0, verified): mode **Concept-only** — Penpot is a full general-purpose design tool; embedding it whole is disproportionate to a plan-view editor. This row is explicitly bounded to (a) a written concept spec of which specific Penpot interaction patterns (selection, resize handles, snap-to-grid) map onto `Space3D.tsx`'s currently-fixed-geometry Plan/Front/Side tabs, and (b) a minimal viable adaptation of ONE such pattern as a proof of concept — not a full vector-editor rebuild, and not any copied Penpot code (MPL-2.0's copyleft terms apply to Penpot's own source, avoided entirely by pattern-study rather than code reuse). Acceptance: the concept spec document exists and names the specific interaction patterns adapted; the one minimal-viable pattern (e.g. snap-to-grid on a single draggable element) is demonstrably working on the deployed edge — not just described. | — | **DONE — ATLAS audit `fc792c466`.** `docs/PENPOT_INTERACTION_SPEC.md` lands the decision (no Penpot code embedded) + pattern-mapping table (selection/handles/keyboard-cycle adapted; resize/snap-to-grid explicitly deferred, correctly gated behind a future SUTRA intent per RULE 50). Code: `Space3D.tsx` adds a view-only `BoxHelper` selection outline + `[`/`]` keyboard cycling, no mesh mutation. Live-verified on the deployed edge: `aria-label` updated with bracket-key instructions, `data-selection-pattern="outline-handles"` present, keyboard cycling confirmed changing `data-selection-target` (Level 3 slab → Podium) across two `]` presses. |
| W-77 | CONCEPT_SPEC_BITWARDEN | written concept spec (zero-knowledge/client-side-encryption architecture) for a future document-vault feature | ATLAS | Per `docs/TECH_SCOUT.md` #5 (`bitwarden/clients`, GPLv3 lineage per Bitwarden's actual license despite GitHub's `NOASSERTION` report, verified): mode **Concept-only** — the reusable idea is the end-to-end encryption architecture (zero-knowledge vault, client-side crypto before upload), not Bitwarden's own code; embedding actual Bitwarden client code is explicitly out of scope, since its GPLv3 lineage would impose copyleft obligations on anything that statically links it. This row is bounded to a written concept spec only — no vault feature is built in this pass; the spec is the deliverable. Acceptance: the concept spec document exists, names the specific architecture pattern (zero-knowledge, client-side encryption before upload) as the reusable idea, and explicitly states that no Bitwarden code is to be adapted or linked. | — | **DONE** — `docs/CONCEPT_SPEC_BITWARDEN.md` landed at `d9f18959`. Names the pattern (key derivation client-side only, encrypt-before-upload, decrypt-after-download, server-compromise-yields-nothing, honest key-recovery trade-off) and states no Bitwarden code is adapted or linked. No code shipped in this pass, per the row's own acceptance — the spec is the entire deliverable. |
| W-78 | CONCEPT_SPEC_PRIMEINTELLECT | written concept spec (distributed/decentralized LLM inference infrastructure) for the future grounded-LLM layer | (any seat — owner-agnostic, first to pull once W-31's gate clears) | Per `docs/TECH_SCOUT.md` #8 (`PrimeIntellect-ai/protocol` + `prime-diloco`, Apache-2.0, verified): mode **Concept-only, and gated** — no work should start here until W-31 GROUNDED_LLM_LAYER's underlying LLM-seat approval exists; W-31 is explicitly `ROADMAP-LABEL`, not a pull-eligible row under RULE 35, per its own status. This row's Deps column names W-31 for exactly that reason — RULE 35(2)'s own "deps DONE" pull condition keeps this row correctly un-pullable until that approval exists, without needing a separate STUCK status. Acceptance (once unblocked): the concept spec document exists and names how PrimeIntellect's distributed-inference pattern would apply to W-31's retrieval-grounded language layer over SUTRA's deterministic engines. | W-31 | READY (Deps unmet — see gate above; not actually pull-eligible under RULE 35(2) until W-31 clears) |
| W-79 | RESPEC_ENVIZ (UNBLOCKED 2026-09-06 — split into W-79a..g below) | none — this umbrella row itself carries no envelope; each sub-row is its own claim | (see sub-rows W-79a..g) | Per `docs/TECH_SCOUT.md` #13: a live `gh api`/`gh search repos` check for "enviz-style immersive 3D web setup" found no canonical matching repo — GitHub search for "enviz" returns unrelated projects (a pentest network-visualization tool, IBM's "Envizi" emissions-accounting product, an enrollment-visualization class project); none concern immersive 3D web scenes. Per RULE 27/39 (never fabricate a missing citation's substance): this is recorded as a **study topic, not a verifiable GitHub target**, and marked STUCK rather than READY, since "owner-agnostic, any seat may pull" cannot apply to a target that does not yet exist in confirmed form — no seat can productively execute against an unverifiable reference. **Single blocking question (per RULE 37, to be posted to docs/OPERATOR_INBOX.md in the same pass):** what specific reference site/repo/demo was meant by "enviz" — a concrete link or exact name is needed before this row can be scouted or built against for real. | — | RESOLVED — unblocked 2026-09-06 by the operator's own re-specification (see docs/OPERATOR_INBOX.md); split into W-79a..g below, each its own claim under RULE 35. This umbrella row itself carries no envelope and is not pull-eligible. |
| W-79a | WALK_MODE | first-person camera mode in the existing three.js cockpit canvas | (any seat — owner-agnostic, first to pull) | A first-person, 1:1-scale walkthrough mode inside the existing cockpit 3D canvas: an eye-height camera (not the default orbit/overview camera), pointer-lock mouse-look (per RULE 50's carve-out framing — pointer-lock look/movement is a VIEW operation, not a model mutation, consistent with orbit/pan/zoom already being view-only), and door passages that let the camera move between rooms/floors rather than clipping through walls. This is a new camera/navigation mode layered onto the existing render, not a new 3D engine or a separate viewer. Acceptance: entering walk mode shows a first-person eye-height view of the current model; pointer-lock mouse-look and WASD-or-equivalent movement work; passing through a modeled door transitions between rooms without wall-clipping, verified on the deployed edge. | W-51, W-66 | READY |
| W-79b | INGESTION [PRODUCT: CROSS] | new IFC/Speckle import pipeline (Worker route + client-side parser wiring) | (any seat — owner-agnostic, first to pull) | External 3D model upload via open, non-proprietary paths only: IFC (buildingSMART's open BIM format) direct import, and Speckle connectors for formats Speckle already bridges (`.rvt` Revit, `.skp` SketchUp, `.max` 3ds Max) — no proprietary-format parser is built from scratch in this row. Ferrum's own natively-generated plans (W-55/W-66's plan-generation engine output) continue to extrude directly into the 3D scene with no upload/import step at all — this row adds an alternate ingestion path for externally-authored models, it does not change how Ferrum's own generated plans reach the canvas. Acceptance: an IFC file uploads and renders correctly in the cockpit canvas; at least one Speckle-bridged format (whichever is easiest to source a real test file for) round-trips through a Speckle connector and renders; Ferrum-native plan generation is confirmed unaffected, verified on the deployed edge. | W-51, W-55 | READY |
| W-79c | AR_SITE | WebXR AR module + USDZ export for iOS Quick Look | (any seat — owner-agnostic, first to pull) | 1:1-scale on-site AR projection of the model via WebXR's AR module, on browsers/devices that support it (Android Chrome and similar) — an honest capability-detection chip where WebXR AR isn't available, never a silent no-op. For iOS, which lacks WebXR AR support, a USDZ export feeds Apple's native AR Quick Look viewer as the fallback path, not a degraded/fake AR experience. Acceptance: on a WebXR-AR-capable device, the model projects at 1:1 scale anchored to a real surface; on iOS, tapping the AR entry point opens the USDZ file in Quick Look and the model displays correctly there; devices supporting neither show the honest capability chip, verified on real devices per RULE 41's floor-device protocol. | W-51 | READY |
| W-79d | ANNOTATE [PRODUCT: CROSS] | in-space measurement/note UI + D1 schema for persisted annotations with revision history | (any seat — owner-agnostic, first to pull) | In-space measurement (point-to-point distance, matching the model's real dimensions per RULE 30's dual-unit convention) and free-text notes anchored to a specific point/element in the 3D space. Both persist to D1, not local-only state, and carry a revision history — an edited or deleted annotation keeps its prior versions retrievable, not silently overwritten. Acceptance: a measurement and a note both persist across a page reload (confirmed reading from D1, not from local cache); editing a note preserves and shows its revision history, verified on the deployed edge. | W-01 | READY |
| W-79e | LIVE_TOUR [PRODUCT: CROSS] | Cloudflare Workers WebSocket session + shared-camera/voice-note sync | (any seat — owner-agnostic, first to pull) | Real-time guided tours: a host's camera position/orientation broadcasts to connected viewers via a Cloudflare Workers WebSocket session (Durable Objects or equivalent, whichever the implementer determines fits the existing Worker architecture — this row does not pre-decide the exact primitive), with voice-note sync so the host can narrate live. This is a shared, synchronized viewing session, not a recorded/async video. Acceptance: a second connected client's camera view visibly follows the host's in real time (not polling-lag levels of delay — a genuinely live sync); a host's voice note is heard by connected viewers in the same session, verified with at least two real concurrent clients on the deployed edge. | W-51 | READY |
| W-79f | MATERIAL_SWAP | live finish/material swap UI extending the existing option-chip mechanism (W-28/W-48) | (any seat — owner-agnostic, first to pull) | Live finish/material switching on modeled surfaces (e.g. flooring, wall finish, roof material) directly in the 3D view, extending the existing option-chip mechanism (W-28's guided chips / W-48's registry, not a new, parallel control system) rather than introducing a separate UI. Per RULE 50/W-53's carve-out framing: a chip-driven swap is not a mouse-mutation violation (it routes through the same intent pipeline as every other chip), and if a slider-style swatch picker is used instead, it must carry a paired input per W-53's own pattern. Acceptance: selecting a material option visibly and correctly changes the corresponding surface's appearance in the 3D view in real time, verified on the deployed edge; the swap routes through the same chip/intent pipeline as other option-chip interactions, not a bespoke one-off handler. | W-28, W-48, W-53 | READY |
| W-79g | FLYTHROUGH [PRODUCT: CROSS] | automated camera-path system + MediaRecorder-based export pipeline | (any seat — owner-agnostic, first to pull) | An automated camera-path flythrough of the model (a pre-defined or auto-generated path through/around the building, not a manual recording of a live walk-mode session), rendered and captured via the browser's `MediaRecorder` API into a downloadable video file — no server-side video-rendering pipeline required for the first pass. Acceptance: triggering a flythrough plays an automated camera path through the model; the resulting recording exports as a real, playable video file (not a broken/empty capture), verified on the deployed edge. | W-51, W-79a | READY |
| W-27 | CONVERSATIONAL_PRIMARY | command-bar UI (text + voice input) — MASON's piece; intent grammar + deterministic-engine dispatch — CRANE's piece; existing slider-based default view (demoted, not deleted, into More → Advanced) | MASON (UI) + CRANE (grammar) | The command bar (per W-09) becomes THE primary interface, not one option among several: text input plus voice input (browser Web Speech API where supported; an honest chip/fallback where it isn't — RULE 5/29 style, no silent no-op). Voice transcript feeds the same intent pipeline as typed text, not a separate path. Intent grammar (per WORKSPACE_SPEC.md §5) is extended to the full parameter set: floors, plot width/depth, setback, use, room bias, and free-form adjustments like "make it Vaastu-friendlier." Every resolved intent drives the deterministic engine (W-24's COMPLIANCE_ENGINE where relevant, the existing structural/massing engines otherwise) and the building reshapes live in the 3D space — not a form submit, a live update. Sliders are REMOVED from the default cockpit view; they relegate to More → Advanced (per RIVET's `MoreDrawer`), for power users, not deleted from the app. **Amended by W-48 (2026-09-04):** this row's "all sliders to Advanced" default is superseded — W-48's registry-driven controls become the visible unique manipulation layer in the default view alongside conversation; only raw, non-registry sliders still relegate to Advanced. **Superseded again by W-53 (2026-09-05, RULE 44), then W-53 itself REVERSED (2026-09-05, operator refinement, "latest wins"):** the "Advanced" carve-out ban is no longer in force. Per W-53's reversal, sliders (including W-48's registry-rendered ones) are visible again, each with a paired numeric input beside it; both feed the same intent pipeline as SUTRA. SUTRA remains primary; sliders+paired-inputs are a secondary path, not a return to a slider-only default. Acceptance: a typed and a spoken instance of the same intent produce the same reshaped result on the deployed edge; sliders visible on the default view each carry a paired numeric input (metric+imperial). **Perf-delta check added (RULE 41(4), 2026-09-04):** before/after bundle-size comparison + fps probe against `budgets.json` (W-34); a regression blocks this row's landing. | W-09 | DONE — `525430b2` ("[land:w2-401-w27-w28]"), touching CommandBar.tsx, Space3D.tsx, WorkspaceCockpit.tsx (2026-09-04 reconciliation). Perf-delta and live-edge verification not independently re-confirmed by SCRIBE. |
| W-28 | GUIDED_OPTIONS | option-chip UI surfaced by the command bar/cockpit when no text/voice input is given; option generation logic reading the ruleset (W-24 COMPLIANCE_ENGINE + existing FAR/DCR/setback data) | RIVET or MASON | When the user gives no text/voice input, the site offers constrained option chips, one decision point at a time, in this order: use → floors → massing style → rooms split → compliance add-ons. Each option set is derived from the ruleset, not free-form — only legally/feasibly buildable options are offered (e.g. max floors computed from FAR, setbacks computed from DCR for the parcel in context), so an infeasible choice is never presented as tappable. One tap reshapes the building live, same deterministic-engine path as W-27's typed/voice intents. This is the flow's stand-out claim: the website does the modelling, the user picks constrained real options rather than operating raw controls. Acceptance: for a given parcel, the offered floor-count options on screen match what the ruleset actually permits (spot-checked against W-24's output, not just the UI's own claim); one tap visibly reshapes the model on the deployed edge. **Perf-delta check added (RULE 41(4), 2026-09-04):** before/after bundle-size comparison + fps probe against `budgets.json` (W-34); a regression blocks this row's landing. **Acceptance amended 2026-09-05 (RULE 44, applies to SUTRA per W-40 on every route/preview):** SUTRA must expose the FULL guided tree as chip groups, in conversation order — use → floors → massing style → setback/coverage → rooms split → material grade → compliance add-ons (Diligence/Permits) → extract/export/share — each choice state-derived and ruleset-constrained (not a static, order-fixed form). After every state change, SUTRA offers context-aware "next question" suggestions (not a fixed script). SUTRA also runs a KB-answer mode: a domain question gets answered with a real clause citation (per W-29/W-41), not a bare number. The W-39/demo-mode loop (MASON's scripted intent cycle) must cycle DISTINCT intents (floor, use, setback, BOQ extract) — never repeating the same intent as if it were the whole demo. **Re-verification flagged:** this row was already marked DONE via reconciliation against commit `525430b2`; that landing predates this amended acceptance, so DONE status here reflects the pre-amendment scope only — whichever seat next touches this row should re-verify against the amended acceptance before treating it as fully current. **Superseded by W-58 (2026-09-05, per operator):** W-58 ARCHITECT_INTAKE_TREE replaces this row's guided-options chip flow with a full branching architect-intake decision tree (site/family/program/budget/style/interior/culture/future) as the entry surface; this row's underlying option-chip rendering mechanism and ruleset-constrained ("only legally buildable options offered") principle carry forward into W-58's tree nodes rather than being discarded — W-58 is the new authoritative row for this interaction going forward. | W-24 | DONE (pre-amendment scope) — `525430b2` ("[land:w2-401-w27-w28]"), same commit as W-27 (2026-09-04 reconciliation). Perf-delta, live-edge, and the 2026-09-05 amended-acceptance items are not independently re-confirmed by SCRIBE. Superseded by W-58. |
| W-29 | KNOWLEDGE_BASE | new versioned data module(s) — structured code corpus, not scraped/copied text | CRANE + MASON | A versioned, structured code corpus covering: NBC 2016 core clauses, IS 456/875/1893 key tables, SP 7, four city DCR samples, and dimensional-standards tables — as our own structured data (fields/tables we author), not copied source text. Every fact carries a clause ID, a version tag, and a status chip (VERIFIED-SAMPLE or INDICATIVE per RULE 5/29 — no fact reads as more authoritative than its actual sourcing). Assistant answers (W-27's conversational layer) and W-28's option chips cite the specific clause when relevant (e.g. "IS 456 §26.5.3", "DCR 2025 T4.2") rather than asserting a number with no traceable source. Split envelope: CRANE and MASON each claim their own structural slice under RULE 35, not a joint claim. **Scope amended by W-60 (2026-09-05, KB_MAX_DEPTH):** this module's depth target extends to the last available public data per domain, via a continuous adapter pipeline + operator REFINE slot — see W-60 for the full amendment; this row's own "as our own structured data" and clause-ID/status-chip conventions stay unchanged and are what W-60 deepens. | — | READY |
| W-30 | VOCAB_ONTOLOGY | new ontology/terminology-mapping module, consumed by the intent parser and reply templates | MASON | A professional-terminology ontology, mapping equivalent terms across the stages a user moves through: setback = margins = build-line; FAR = FSI = plot-ratio; sanction = plan-approval; limit-state/spans/loads (structural vocabulary); fixtures/connected-load (MEP vocabulary); NOC/OC (approvals vocabulary); IRR/ticket (finance vocabulary). The intent parser (W-27's grammar) and the assistant's replies both resolve through this ontology, so the site speaks each professional's own language at each stage rather than forcing one vocabulary on every user. | W-27 (grammar half) | READY |
| W-31 | GROUNDED_LLM_LAYER | n/a — roadmap only, no envelope claimed until an LLM seat trial is approved | (unassigned) | When a trial LLM seat is approved (a separate, future decision — not implied or pre-approved by this row), that seat acts strictly as a LANGUAGE layer over the existing deterministic engine and W-29's knowledge base: retrieval-augmented generation with clause citations (per W-29), never a source of a number or an option on its own. **Status: ROADMAP-LABEL, as given** — not a pull-eligible row under RULE 35 until the underlying LLM-seat approval exists. | W-29 | ROADMAP-LABEL |
| W-32 | BATTERY_FAILS | three independent fix rows, one per failing check — see sub-rows below; each is its own envelope, not a joint claim | (per sub-row) | Umbrella row for ATLAS's 8-step battery (W-10) failures, cited by the operator as check numbers (2), (5), (8). **Flagged, not fabricated (RULE 39):** docs/TASK_BOARD.md's own W-10 row is still READY, with no battery run or results actually logged on disk as of this writing — SCRIBE has not seen the full 8-step battery output, only the three failing item names given directly in this instruction. Recorded as given; whoever ran the battery should log its full results on W-10 for real. | W-10 | (see sub-rows) |
| W-32a | Fix: artifact-appears readback (battery check 2) | the artifact-save→readback path (workspace artifact GET after a POST) | (board-pull — any eligible seat) | A saved artifact reliably appears on readback (GET) immediately after its save (POST), verified against the deployed edge — the specific failure named as battery check (2). | W-32 | READY |
| W-32b | Fix: extract-updates-on-mutate (battery check 5) | the bottom extract panel's data-binding to its source artifact/state | (board-pull — any eligible seat) | The bottom extract panel updates when its underlying artifact/state mutates, without requiring a manual refresh — the specific failure named as battery check (5). | W-32 | READY |
| W-32c | Fix: share-opens (battery check 8) | the share action's generated link/route | (board-pull — any eligible seat) | A share action produces a link that actually opens to the shared content when followed, verified against the deployed edge — the specific failure named as battery check (8). | W-32 | READY |
| W-33 | LANDINTEL_BRIDGE | LandIntel result card (`apps/web/app/products/landintel/page.tsx` + `UlpinMapExplorer`'s result rendering) — RIVET's piece; `/project-workspace/:id` route + cockpit LEFT SIDE PANEL component + pre-seeding logic — CRANE's piece; 3D-space pre-seed wiring (plot grid from parcel dims, proposed building type from forecast) — MASON's piece | RIVET + CRANE + MASON | The LandIntel result card gets a second action alongside the existing SAVE (persists artifact to shelf, via the existing `SaveToWorkspaceButton`, unchanged): **MOVE TO WORKSPACE**, which creates a workspace artifact carrying the parcel context and routes to `/project-workspace/:id`, opening the cockpit for that project. On that route, the cockpit renders a LEFT SIDE PANEL (read-only reference, command bar stays primary) showing the source parcel's full territorial detail: owner, survey no, area (dual units per RULE 30), location, zoning/FAR/coverage, water-body buffer status, NDZ flag, flood-zone, and provenance chips (source + freshness per WORKSPACE_SPEC.md §1's Artifact model). The 3D space starts pre-seeded from this context: plot grid dimensions from the parcel's actual dims, proposed building type from W-24/forecast-module suggestion where available. **Region law updated:** side panel = left edge, 10-tab strip = top, tools ruler = right, 3D space = center, extract panel = bottom (five regions, side panel added as the new one). Acceptance: clicking Move opens the cockpit with the side panel populated, verified at 1366+375 on the deployed edge; the existing Save action still works unchanged. Split envelope: each seat claims its own piece under RULE 35, not a joint claim. **Perf-delta check added (RULE 41(4), 2026-09-04):** before/after bundle-size comparison + fps probe against `budgets.json` (W-34); a regression blocks this row's landing. **Acceptance amended 2026-09-05 (RULE 44 — every surface showing Save shows Open beside it: record card, extract panel, artifacts, previews):** the MOVE TO WORKSPACE action above is renamed/extended to "Open in workspace ⛶", rendered NEXT TO "Save to workspace" (side by side, never Save alone) on every surface that currently offers Save: the LandIntel result card, the extract panel, saved artifacts (workspace shelf), and product-page previews. Save persists the artifact and stays on the current page/surface — unchanged behavior. Open navigates to `/project-workspace` with the parcel/record context preloaded (this row's LEFT SIDE PANEL populated, same pre-seeding logic as today) AND enters FULLSCREEN mode immediately (per W-52) — tabs on top, SUTRA right — in the same click, no intermediate page or second click. Acceptance: a screenshot showing the side-by-side Save/Open buttons, plus a screenshot of the resulting fullscreen cockpit with the parcel/record already loaded, at both 1366 and 375. | W-26, W-24 (for forecast pre-seed), W-52 | READY |
| W-34 | PERF_INFRA | new `budgets.json`, Lighthouse CI config on key routes, a degradation-profile toggle (WebGL capability detection → reduced rendering mode), and a documented real-device spot-check protocol | CRANE | `budgets.json` encodes AGENTS.md RULE 41(3)'s budgets exactly: initial JS ≤350KB gz (cockpit route ≤600KB gz, `three` lazy), LCP ≤2.5s on a 4G floor, CLS ≤0.1, INP ≤200ms, main-thread task ≤50ms, draw calls ≤200, FPS ≥30 floor-device/degradation-profile or ≥60 desktop-class. Lighthouse CI runs these against key routes and fails the build on a budget breach — this is what makes RULE 41 a hard, CI-enforced gate rather than a documented aspiration. The degradation-profile toggle detects WebGL2 vs WebGL1-only vs no-WebGL and switches rendering profile accordingly (shadows/reflections/pixelRatio/viewport-count per RULE 41(2)), functional and honestly labeled either way. The real-device spot-check protocol documents how to verify against RULE 41(2)'s actual floor devices (2022 mid-range Android / 2018 i5+iGPU), not just simulated throttling. Acceptance: `budgets.json` exists and is wired into CI; a deliberately oversized bundle actually fails the CI check (a real negative test, not just the config existing); the degradation toggle demonstrably changes rendering profile on a WebGL1-forced test. | — | READY |
| W-01 | Migration + save200 | D1 migration file(s), the workspace save/persist endpoint | CRANE | Migration applies clean on the deployed D1 instance; a save call returns 200 against the deployed edge, verified by an actual request, not a local smoke test | — | CLAIMED (CRANE) |
| W-02 | Add `three` dependency | `apps/web/package.json`, `pnpm-lock.yaml` | CRANE | `three` installed as the single approved new dependency (per W2-380's operator approval, RULE 1 dependency-addition rule — CRANE-only); build stays green | — | READY |
| W-03 | `lib/types.ts` integration merge | `apps/web/lib/types.ts` | CRANE | Workspace object-model types (WorkspaceProject/Artifact per WORKSPACE_SPEC.md §1) merged into the shared contract file with no breaking change to existing consumers; typecheck green | W-02 | READY |
| W-04 | page.tsx assembly — mount RIVET comps + MASON canvas | the workspace route's `page.tsx` | CRANE | RIVET's rail components and MASON's 3D canvas both mount without collision on the deployed edge; first-viewport screenshot at 1366 + 375 per RULE 24 | W-03 | READY |
| W-05 | Space3D three-integration + gates + proofs | the 3D configurator component(s) (MASON's S4/three.js piece, folded from W2-384 into W2-401) | MASON | Plot-anchored 3D view renders on the deployed edge; RULE 2 structural pass/fail gate wired live; RULE 29/30 numeric/unit obligations verified on-screen; live proof attached | W-02 | READY |
| W-06 | ExportBar — IFC/DXF | export UI component + wiring to `lib/ifc-export.ts` | MASON | Export control produces a real IFC and DXF file from a live workspace artifact, not a stub; verified against the deployed edge. Gated on W-20's bundle-safety result (2026-09-04): if W-20 passes, this row wires the existing writer in as-is; if it fails, this row waits for MASON's browser-only rewrite from W-20 instead | W-20 | READY |
| W-07 | Wire components into workspace route | workspace route wiring (RIVET's rails + CRANE's page.tsx assembly) | RIVET | RIVET's components are live-reachable through the assembled route on the deployed edge; no dead mounts | W-04 | READY |
| W-08 | Intent API | new Worker route implementing WORKSPACE_SPEC.md §5's intent-phrase routing onto §4's contracts | CRANE | Each §5 intent phrase's mapped contract call succeeds against the deployed Worker; 400s behave per §4 | — | READY |
| W-09 | Command bar UI | command-bar component consuming W-08's intent API | RIVET | A typed phrase from §5's list resolves to the correct §4 action, verified live; first-viewport screenshot per RULE 24 | W-08 | READY |
| W-10 | ATLAS 8-step battery | none (audit only — no source envelope claimed) | ATLAS | Independent audit of W-07 and W-09 against WORKSPACE_SPEC.md §6's acceptance checklist; 8-step battery results logged on this row, no self-certification by CRANE/MASON/RIVET | W-07, W-09 | READY |
| W-11 | Workspace shelf EMPTY-STATE | workspace shelf component (signed-out view + sample-artifact CTA) | RIVET | Signed-out visitors to the workspace shelf see a real empty-state (not a bare box) with a sample-artifact CTA; verified on the deployed edge — direct response to the operator's live observation of a bare empty box | — | READY |
| W-12 | Keyboard fit-model control | 3D configurator input handling (keyboard controls for fit-to-model) | MASON | A keyboard shortcut/control fits the model view in the 3D configurator, verified live; accessibility note per RULE 28/29 conventions where applicable | — | READY |
| W-13 | View-state permalinks | workspace view-state serialization + URL routing | MASON | A workspace view's current state (camera/selection/artifact) round-trips through a shareable permalink URL, verified live | — | READY |
| W-14 | AQ-RIVET-004 app-link diagnostic | mobile app-link wiring (apps/mobile/**, per RIVET's exclusive paths) | RIVET | Diagnose and fix the app-link issue per AQ-RIVET-004. Note: the underlying AQ-RIVET-004 proposal's actual text is not on disk in this session — SCRIBE has not seen its full scope, only the label given. RIVET should confirm scope against its own proposal record before executing, per the same practice used for AQ-RIVET-001 | — | READY |
| W-16 | LANDINTEL RESTORE | `apps/web/app/products/landintel/page.tsx` (hero + featureItems/howItWorksSteps/FAQ copy) | RIVET | `UlpinMapExplorer` (real D1-backed ULPIN/Bhu-Aadhaar lookup) returns as the PRIMARY hero tool, with sample chips + Lookup control + a result card carrying its provenance strip; `SteppedForecastModule` (land-use forecast) stays live as a SECONDARY panel, not removed; both tools live and dual-unit (RULE 30) on the deployed edge; honesty chips (INDICATIVE/VERIFIED) present on both. Forensics: commit `331c1b08` (W2-372 UI_UX_MODERNIZATION, 2026-09-04) is confirmed via `git log -S "ULPIN"`/`git show` as the commit that replaced the lookup with the forecast module — logged as a friction entry in docs/TASK_REPORTS.md, not just fixed silently. Acceptance: first-viewport screenshot at 1366+375 shows the lookup first; a real lookup executes against the deployed edge; the forecast module is still present and functional below/beside it. | — | READY |
| W-17 | AUTH-PREVIEW | `apps/web/app/signup/page.tsx`, `apps/web/app/login/page.tsx` (form removal + preview gate); Log in / Start Free Trial CTA targets sitewide; workspace + account surface PREVIEW chip | RIVET | Zero credential inputs (no email/password fields, nothing collected) on both `/signup` and `/login` at 1366+375 — both routes replaced with an honest preview gate carrying the copy "Accounts arrive with the live release — explore everything now in preview" and one "Enter preview" action that sets a `localStorage` session flag only. Every Log in / Start Free Trial CTA sitewide routes to the preview gate, not a credential form. Workspace and account surfaces open in preview mode with a visible PREVIEW chip. Real auth (email+password, hashed, sessions) is explicitly NOT this row's scope — it stays a separate roadmap row for post-live. **Discrepancy flagged, not silently overridden (RULE 27):** docs/WAVE_QUEUE.md's W2-326 AUTH_COMPLETE row already shows DONE with a landing SHA (`4ef78791`, PBKDF2/WebCrypto password auth, sessions, verify/reset flows) — real backend auth code already exists on `origin/main`. This row does not touch or remove that backend; it hides the *frontend* credential-collection surface behind a preview gate per the operator's explicit instruction, pending an operator-set "live release" milestone. RIVET/whoever executes this should not delete or revert W2-326's backend work — only the two page UIs and the CTA routing change. | — | READY |
| W-20 | IFC EXPORT: bundle-safety gate before build-vs-reuse decision | `apps/web/lib/ifc-export.ts` (test/bundle harness only — no production code changes until the gate result is known); `apps/web/components/workspace/ExportBar.tsx` (W-06) is downstream, not this row's envelope | CRANE | **MASON's proposal, verbatim as given to SCRIBE:** a browser-only STEP writer, separating a validation-only `web-ifc` path from the client export path. **CRANE's counter-finding, given as this row's acceptance note:** `lib/ifc-export.ts` already writes STEP/SPF text directly (plain string generation, not `web-ifc`'s typed schema classes, which SCRIBE confirmed by reading the file are noted there as non-constructable at this `web-ifc` version) — so before either proposal is built, Step 1 of this row is a Workers-bundle test proving the *writer* path is actually browser/Workers-safe with zero runtime deps in that path. SCRIBE's own disk check of the file found one real risk worth testing, not assuming away: `getWebIfc()` (used only for the round-trip *verification* path, not the writer) calls `createRequire`/Node's `module` built-in to force-load `web-ifc`'s Node build — a Node-only mechanism that would not survive a Workers/browser bundle if that function or its import got pulled in by the bundler. The test must confirm the bundle only pulls in the writer path, not `getWebIfc()`. If the bundle-safety test passes clean: this row becomes "wire the existing `lib/ifc-export.ts` writer into ExportBar (W-06)," NOT a duplicate rewrite of a browser-only STEP writer from scratch — MASON's proposal is then satisfied by reuse, not new code. If the test fails (the writer path itself pulls in something Node-only): MASON's original browser-only-writer proposal proceeds as planned, since the reuse premise would be false. | — | Step 1 DONE, result FAIL (see TASK_REPORTS.md) — bundler resolves `getWebIfc()`'s `createRequire`/`import('module')` even though the test entry only imported `exportMassingToIfc`, because it lives in the same file. Per this row's own decision rule: MASON's original browser-only-writer proposal proceeds. Flagged alternative (splitting `getWebIfc`/`countIfcGeometry` into their own file) not verified, out of this row's test-only envelope. |
| W-37 | POINTER_SWEEP | site-wide interactive-element styling (nav links, buttons, tabs, chips, cards, sliders, option chips) — cursor/hover/focus-visible states | RIVET | **RECOVERED 2026-09-08: PI's first Execution Ledger review found this row cited in docs/ACTIVITY_LOG.md (commits `84bacef8`/`3bf36fbed`, 2026-09-04) but absent from the current board — a real gap, not intentional, confirmed by checking origin/main directly for any landed pointer-sweep work under any branch name (none found). Restored verbatim per RULE 3 append-only; this is genuinely unlanded work, not superseded by anything since.** Every interactive element site-wide gets `cursor: pointer` plus real hover and `:focus-visible` states; the I-beam text cursor appears ONLY inside actual text inputs, nowhere else. Acceptance: a headless computed-style check confirms `cursor: pointer` on every interactive selector, on every route, across the RULE 41 responsive matrix widths — not a visual spot-check, an actual computed-style assertion. **Perf-delta check (RULE 41(4)):** before/after bundle-size comparison + fps probe against `budgets.json` (W-34); a regression blocks this row's landing. | W-34 | READY |
| W-39 | WORKSPACE_PROMPT | every route's hero/first-viewport section (home, all 10 product pages, pricing, resources, docs, about, the W-17 preview gate) — RIVET's UI piece; the cockpit demo-mode Space3D scripted intent loop (`add 2 floors` → massing grows, `setback 3m` → shifts) — MASON's piece; hero/nav copy — voice per below | RIVET (UI) + MASON (demo-mode) | **RECOVERED 2026-09-08, same PI finding as W-37 — restored verbatim, sequenced ahead of W-38 per its original 2026-09-04 re-sequencing.** **Cross-reference, not a conflict (checked directly before restoring):** W-69 UX_OVERHAUL (2026-09-06) later added a LIVE embedded cockpit teaser to the home hero specifically — this row's demo-mode Space3D loop and W-69's teaser are complementary, not duplicate work, but whoever picks up either row should coordinate to avoid two competing hero-canvas implementations landing separately. W-89 NO_AUTONOMOUS_MUTATION (2026-09-07) now governs this row's scripted intent loop directly: the demo-mode loop must either run on a labelled DEMO scratch model or be restricted to camera/highlight moves — it may NOT silently mutate a real project, which W-89 didn't exist to constrain when this row was first written. On every route named: (1) a first-viewport narrative line selling the outcome — "Walk into your project's workspace: plans, costs, compliance and the living 3D model in one place"; (2) the page's own existing live tool stays exactly as-is, as proof, not replaced or demoted (RULE 29 Feature Conservation addendum applies); (3) a primary CTA into the cockpit in the first viewport — a sticky "Open Workspace" pill plus a nav button beside the existing Start Free Trial button (confirmed real on `apps/web/app/page.tsx`, `pricing/page.tsx`, `products/boq-pro/page.tsx` and others — not invented); (4) an excitement element: a demo-mode Space3D — the real cockpit 3D canvas (not a mockup/video) playing a scripted intent loop as a muted auto-visual, wherever layout allows, now RULE 50/W-89-compliant per the cross-reference above; (5) copy voice is professional, concrete, exciting, and RULE 30-style claim discipline applies to marketing copy specifically: every claim maps to either a live feature or an explicitly labeled roadmap item — no claim reads as live when it isn't. ATLAS honesty-checks the copy against the actual site state before this row is considered DONE, not as an optional follow-up. Acceptance: the prompt appears in the first viewport on every named route at 1366+375; clicking the CTA reaches the cockpit. **Perf-delta check (RULE 41(4)):** before/after bundle-size comparison + fps probe against `budgets.json` (W-34) — the demo-mode 3D canvas is exactly the kind of addition this check exists to catch a regression from. | W-34, W-89 | READY |
| W-38 | UI_MODERNIZATION_PASS | nav chrome, product hero sections, cockpit chrome — visual/interaction polish only, no logic changes | RIVET | **RECOVERED 2026-09-08, same PI finding as W-37/W-39 — restored verbatim, still sequenced after both per the original 2026-09-04 ordering.** **Cross-reference, not a conflict (checked directly before restoring):** W-69 UX_OVERHAUL (2026-09-06) later covered overlapping ground — soft-shadow/glass accents, type-scale contrast, sliding tab-underline, button press feedback — on the marketing site's hero/journey-strip/product-cards specifically. This row's own scope (nav chrome, product hero sections, cockpit chrome broadly, plus skeleton loading states which W-69 does not mention) is broader than and not fully subsumed by W-69 — whoever picks up either row should check the other's landed state first to avoid re-doing the same nav/hero polish twice. RIVET researches current (2026) UI/UX conventions live before applying anything — not from memory alone — covering: hover elevation, nav underline-slide, 150–250ms eased micro-transitions, an 8pt spacing rhythm, a modern type scale, skeleton loading states, and `prefers-reduced-motion` respected throughout. Applied to nav, product hero sections, and cockpit chrome, keeping the existing brand (dark navy + saffron) — this is a polish pass on top of the existing design system, not a redesign, and per AGENTS.md RULE 29's Feature Conservation addendum must not remove or demote any currently-live tool while restyling around it. Acceptance: before/after screenshots at the RULE 41 responsive matrix; CLS ≤0.1 (RULE 41(3)); full RULE 41 budget pass, not just the CLS figure in isolation. | W-37, W-39, W-34 | READY |
| W-40 | SUTRA_SIDE_PANEL | cockpit region-law restructure — new persistent full-height right-side chat panel (desktop) / bottom-sheet (mobile); tools ruler relocates to a slim strip at the canvas-left inside the center region | MASON + RIVET | The agent becomes a persistent full-height chat panel occupying the RIGHT side of every route — default OPEN on desktop, not hidden behind a click; a bottom-sheet on mobile. Header reads "SUTRA — your digital Sthapati" plus honesty chips. The chat itself is the existing W-27 engine (text + voice + option chips + KB citations) mounted into this new panel location, not a new/separate chat implementation. **Cockpit region law updated (2026-09-04):** right = SUTRA panel (new); tools ruler relocates from its prior right-edge position to a slim strip at the canvas-left, inside the center region; left = territorial panel (W-33, unchanged); bottom = extract panel (unchanged); top = 10-tab strip (unchanged). Split envelope: MASON and RIVET each claim their own piece under RULE 35, not a joint claim. Acceptance: SUTRA panel is visibly open by default on desktop at 1366, collapses to a bottom sheet at 375; the relocated tools ruler is still fully functional at its new position; the existing W-27 chat engine behaves identically inside the new panel. **Perf-delta check (RULE 41(4)):** before/after bundle-size comparison + fps probe against `budgets.json` (W-34); a regression blocks this row's landing. **Region law amended by W-63 (2026-09-05, fullscreen only):** in W-52's fullscreen mode specifically, the SUTRA panel occupies exactly 1/3 of the viewport width on the right, with the cockpit (canvas + tabs) occupying the remaining 2/3 — this fullscreen-specific ratio does not change this row's normal (non-fullscreen) region law above. | W-27, W-33, W-34 | READY |
| W-41 | KB_EXHAUSTIVE | extends W-29's knowledge-base module(s) with a public coverage manifest | CRANE | Extends W-29 KNOWLEDGE_BASE with a public coverage manifest spanning the whole architecture subject: design theory, planning, RCC/steel/masonry structure, soil & foundation, MEP, materials, quantities & rates, contracts & tendering, approvals/NOCs per city, finance, sustainability, safety, and Vaastu (explicitly labeled traditional practice, not code — never presented as a building-code requirement). Every topic module carries clause-level facts, each with its own ID and status chip (per W-29's VERIFIED-SAMPLE/INDICATIVE convention). The manifest itself shows seeded vs. roadmap topics honestly — a topic with zero seeded facts shows as roadmap, not as silently absent or falsely implied complete. Acceptance: the manifest is queryable/viewable and its seeded/roadmap split matches the actual fact count per topic, not a hand-typed claim. **Scope amended by W-60 (2026-09-05, KB_MAX_DEPTH):** this row's coverage manifest becomes the base W-60 extends with a per-domain depth-percentage figure, audited by ATLAS — see W-60 for the full amendment. | W-29 | READY |
| W-42 | PRELOOKUP_CARD | LandIntel result-card component (`apps/web/app/products/landintel/page.tsx` + `UlpinMapExplorer`) — pre-lookup state | RIVET | Before any lookup is run, LandIntel shows the FULL record-card structure already populated with PREVIEW-labeled sample values, carrying source/freshness chips exactly as a real result would — so the user sees the shape of what they'll get before committing to a lookup. Running an actual lookup swaps the PREVIEW sample values for real ones without changing the card's structure. Acceptance: the pre-lookup card renders with every field present and clearly marked PREVIEW/sample; a real lookup replaces the values in place, same layout. | — | READY |
| W-43 | MATERIALS_CATALOG | new catalog data module(s) — CRANE's piece; catalog UI (item list, coverage manifest, missing-item request loop) — MASON's piece | CRANE (data) + MASON (UI) | **ONE-LIBRARY architecture note (amended 2026-09-04, applies before any consumer reads this catalog):** this materials catalog is the single source of truth for three data layers on every item — commercial data (Indian household brand options, a dated price with a PRICE-DATE stamp + source chip, and a vendor-API slot), technical data (IS-code references), and parametric data (dimensions/performance characteristics, added per this amendment as a first-class field, not an afterthought). Three downstream consumers read from this one library, never maintain their own copy: DesignStudio reads parametric data for plug-and-play elements, Structura reads technical/parametric data for strength/density checks, and BOQ (W-44) reads commercial + technical data for measured rates — **W-44 reads W-43 only, it does not maintain a second catalog.** Per-item fields: category, technical data (IS-code refs), parametric data (dims/performance), unit, brand options (cement: UltraTech/Ambuja/ACC; steel: Tata Tiscon/JSW; paint: Asian Paints/Berger/Nerolac; electrical: Havells/Polycab/Finolex/Anchor; plumbing: Astral/Supreme/Ashirvad; sanitary: Jaquar/Hindware/Cera; tiles: Kajaria/Somany; locks: Godrej; etc., as given), a dated price with PRICE-DATE stamp + source chip (SAMPLE 2026-09, DSR-2025-based), and a vendor-API slot (`price_source: SAMPLE \| VENDOR_API`, the same field W-45's future adapter switches). Seed: a comprehensive curated catalog on the DSR/CPWD item taxonomy, roughly 500-1000 items (tap to main door, brick to paint, as given — an order-of-magnitude target, not a literal count SCRIBE verified item-by-item). A public coverage manifest (category → item count → status) shows real coverage honestly — no fake completeness — plus a missing-item request loop so users can flag gaps. Acceptance: the coverage manifest's counts match the actual seeded catalog, not a hand-typed claim; every item carries all three data layers (commercial/technical/parametric) or is explicitly marked incomplete on that layer, never silently missing one. **Scope amended by W-60 (2026-09-05, KB_MAX_DEPTH):** this catalog's manufacturer/brand depth extends to full brand-catalog/TDS depth per the continuous adapter pipeline + operator REFINE slot — see W-60 for the full amendment; the existing PRICE-DATE/source-chip/vendor-API-slot conventions stay unchanged and are what W-60's pipeline feeds. | — | READY |
| W-44 | BOQ_MEASURED | workspace BOQ computation module — reads W-43 only, no second catalog | MASON | The workspace BOQ computes itemized measured quantities from the plan/massing crossed against W-43's catalog prices — **W-44 reads W-43 only, per the ONE-LIBRARY note on that row; it does not maintain or duplicate catalog data.** Each BOQ line cites its spec (from W-43's technical data), its brand options (from W-43's commercial data), its price provenance (PRICE-DATE + source chip, inherited from W-43), and GST. The existing product-page slider stays exactly as the indicative teaser it already is — this row does not touch or replace it, only the workspace BOQ is new. Acceptance: a generated BOQ's line items are traceable back to specific W-43 catalog entries (not independently invented figures); GST is computed and shown per line. | W-43 | READY |
| W-45 | VENDOR_PRICE_API | n/a — roadmap only, no envelope claimed pre-launch; the adapter contract itself (`price_source` field + interface) is defined now as part of W-43's schema | (unassigned, post-launch) | **Status: ROADMAP, post-launch, as given.** The adapter contract — the `price_source: SAMPLE \| VENDOR_API` field and its interface — is defined now, as part of W-43's schema, so no future migration is needed to add it later. Live vendor feeds switch individual items from SAMPLE to LIVE with vendor-attributed provenance chips once real vendor partners onboard — a separate, future integration effort, not implied or scheduled by this row. | W-43 | ROADMAP-LABEL |
| W-46 | RATE_ENGINE | new source-adapter pipeline module — pluggable, no source-specific code hardcoded into the engine itself | CRANE | A pluggable source-adapter pipeline for streamed rates: adapters for government rate-list PDFs, vendor APIs, and operator-supplied manual feeds, each implementing the same adapter interface so the engine itself stays source-agnostic. Every streamed rate carries a source ID, a fetch date, and a status chip — one of VERIFIED-PUBLIC, REFERENCE-ONLY, CONCEPT, or LIVE (as given; note this is a distinct status vocabulary from W-29/W-43's VERIFIED-SAMPLE/INDICATIVE/SAMPLE convention — RATE_ENGINE's rates are a different data class from the KB/catalog and get their own chip set, not a forced reuse of the other one). The engine ships with zero real sources wired in — the operator defines the actual sources later, and the engine goes live for a new source by adding an adapter, not by rewriting the engine. Acceptance: at least one adapter (any of the three types) is demonstrably pluggable end-to-end against a real or realistic fixture, proving the interface actually works, not just that it's declared; the engine correctly refuses/labels a rate with no adapter behind it rather than fabricating one. | — | DONE — `22f78aa5` ("[land:crane/w46-rate-engine]") + `76dcb42f` ("[task:W-46] real PDF extraction for the CBIC adapter, 4 items promoted to VERIFIED-PUBLIC") (2026-09-04 reconciliation). |
| W-48 | PLUGGABLE_CONTROLS | new tool/slider registry (config-driven schema + renderer) | MASON | A config-driven tool/slider registry: each control is defined as `{id, plain-language label, type: slider\|chip\|toggle, param mapping, min/max/step sourced from the ruleset (W-24), a layman help line}`. The cockpit UI renders controls from this registry — adding a new tool means adding a registry entry, never a code change. The default registry set is deliberately non-technical ("How tall?", "How much open ground?", "How many families?" — as given, not engineering jargon). **Supersedes W-27's sliders-out-to-Advanced default (2026-09-04):** conversation (W-27's text/voice engine) stays the primary interaction mode, but the registry-driven controls are now the visible unique manipulation layer in the default cockpit view — W-27's row is amended accordingly; raw, non-registry sliders still relegate to More → Advanced, but registry controls do not. **Superseded by W-53 (2026-09-05, RULE 44 NO_SLIDERS_ANYWHERE), then W-53 itself REVERSED (2026-09-05, operator refinement, "latest wins"):** the registry's `type: slider` renderer is reinstated — every registry-driven slider now renders visibly, WITH a paired numeric input beside it (metric+imperial per RULE 30), both feeding the same intent pipeline. The registry is still consumed through SUTRA's guided tree as an alternative path, but a visible slider+paired-input is no longer forbidden. Acceptance: adding a new control is demonstrated via a registry-entry-only change (no code touched) and the new control renders as a slider+paired-numeric-input pair, also reachable through SUTRA's guided tree; min/max/step values for at least one ruleset-bound control are verified to actually match W-24's ruleset output, not hardcoded separately; both the slider and its paired input produce identical mutations when set to the same value. | W-24, W-27 | READY |
| W-50 | HARNESS_24x7 | `docs/FLEET_SEATS.json` (seats config, adapters, revive templates), `scripts/FLEET_WATCH.ps1` v3 (Codex/Claude revival adapters, `Get-CodexResetTime`, `Test-DueForRevival`, `Get-TopReadyRow`, `-DryRun`) | CRANE | **RECOVERED 2026-09-08: PI's first Execution Ledger review found this work landed (`fcfc892f`, "[land:crane/w50-harness-24x7]") but with no board row of its own — CRANE-authored fleet-harness infrastructure that was never seeded through SCRIBE's own pull-queue, a governance gap flagged but left unresolved earlier this session (see this file's own Notes history). Seeded now, retroactively, as DONE (landed), not READY, since the work is real and already on `origin/main` — confirmed directly, not assumed.** Seat-agnostic revival harness: a Codex adapter using exact recorded provider reset times, a gated-but-wired Claude adapter (`-EnableClaudeAdapter`), and `Get-TopReadyRow` which reads this very board and returns the first READY row in table order (the harness's own de facto definition of "priority order," since no separate priority column exists on disk). RULE 45/46's drain-don't-wait and idle-only-with-enquiry rules, and RULE 51/52's no-manual-gate and open-drain rules, all depend on this harness's revival/detection mechanisms actually working as specified. Acceptance (retroactive, based on the landed commit's own stated scope): `docs/FLEET_SEATS.json` correctly enumerates all seven seats with real, disk-verified worktree globs (not invented for seats with none observed); `Get-TopReadyRow` is confirmed to actually read this board's table order, not a separate priority scheme. | — | DONE — `fcfc892f` ("[land:crane/w50-harness-24x7]"). RULE 25 deployed-edge/operational live verification (an actual observed auto-revival event) is a separate, still-open concern SCRIBE has not itself performed — this DONE marks landing, not an operational-proof claim. |
| W-51 | ONE-GROUND (amended 2026-09-05 from SATELLITE_GROUND, per RULE 44 principle-generalization) | cockpit ground rendering across all 10 tabs — MASON's 3D piece; every product-page preview's ground reuse; imagery-source adapters + ToS due-diligence — CRANE's piece | MASON (3D) + CRANE (sources) | **Generalized per RULE 44:** the live satellite ground (imagery + boundary + OSM-extruded existing buildings + attribution/date chips) is the BASE LAYER of the cockpit on ALL TEN TABS, and on every product-page preview (the "W-36" reference traces to MASON's real, landed `mason/w36-<product>` branch series across the product pages — not a seeded board row; flagged, not assumed). Tabs switch the LENS on top of this one shared ground, never re-render a separate ground: Land = reality only (the original SATELLITE_GROUND scope); Design = proposed massing overlaid on it; Structure = frame/spans overlaid on it; Cost = BOQ zones overlaid on it; Market/Procure/Invest/Build/Community/Transact = each product's own data lens overlaid on the same real site. Source order, compliance-first as given: Bhuvan/ISRO (govt, open) → Esri World Imagery (attribution required) → EOX Sentinel-2 cloudless. **CRANE does real ToS due-diligence for commercial use before any source is wired in** — gates which source ships as the default, not pre-decided by this row. The shipped default carries an attribution chip and an imagery-date chip, always visible on every tab, never omitted. Existing buildings render as OSM footprints extruded, carrying a chip reading "existing-from-OSM, not a survey." Empty plots show imagery + boundary outline only. Sample parcels get real reference coordinates labeled SAMPLE LOCATION. **Offline/failure path:** an honest placeholder grid + a chip explaining imagery is unavailable — never a fake or stale image. **Failure mode, explicit:** any tab or product-page preview rendering the generic grid instead of the parcel ground is a FAIL, not a partial pass. Acceptance: the ground (imagery/boundary/OSM buildings/attribution/date chips) is verified present and identical across all 10 tabs and product-page previews at 1366+375; only the lens overlay differs per tab; ATLAS's audit battery checks every tab/preview, not just the Land tab. | — | READY |
| W-53 | NO_SLIDERS_ANYWHERE (amended 2026-09-05 — see reversal below; title kept for row-history continuity, behavior is no longer a slider ban) | every product-page preview + DesignStudio's plot-width/plot-depth/floors sliders (`apps/web/app/products/designstudio/**`) — RIVET's piece; cockpit route + all ten cockpit tabs + fullscreen (W-52) manual controls — MASON's piece; paired numeric-input component (metric + imperial) wired beside every slider — RIVET's piece | RIVET + MASON | **REVERSED 2026-09-05 (operator refinement, explicitly "latest wins" — this supersedes the original ban stated below, which is kept for history, not deleted, per RULE 3 append-only):** Sliders are NOT removed. Every slider that exists or would have existed under this row's original text instead gets a PAIRED numeric input rendered beside it — the user can type an exact value (metric AND imperial shown together, per RULE 30) instead of dragging. Both the slider and its paired numeric input feed the same parameter through the same intent pipeline W-27/SUTRA already uses — neither is a separate, ungoverned mutation path; a value set either way is validated against the ruleset (W-24) the same way a SUTRA-driven change is. SUTRA remains the primary interaction mode (per W-27/W-58) — the paired slider+input is a secondary, direct-manipulation path for users who prefer it, not a replacement for SUTRA. **RULE 44 (seeded pre-generalized per the operator's own instruction):** applies to every slider on every surface — product pages, DesignStudio, cockpit (all ten tabs), fullscreen — uniformly, not selectively. **RULE 50 conflict, flagged not silently resolved:** RULE 50 (MOUSE_VIEW_ONLY, adopted earlier the same day) states mouse/touch gestures on 3D/model surfaces are VIEW-only and every mutation flows through SUTRA; a mouse-dragged slider mutating a parameter is a literal instance of what RULE 50 was written to ban. Per RULE 27 (resolve, don't ask) applied to this explicit, timestamped "latest wins" operator instruction: proceeded under the operator's own explicit precedence rather than holding, and logged an explicit carve-out on RULE 50 itself (see AGENTS.md) rather than silently overriding it — RULE 50 still governs every other mouse gesture (orbit/pan/zoom/nav) and every surface without a paired slider+input. Original ban text (2026-09-05, superseded above): "Every visible `<input type="range">`... is REMOVED from: all product pages, all cockpit previews, the cockpit route (all ten tabs), and fullscreen (W-52)... DesignStudio's plot-width/plot-depth/floors sliders are replaced with a single SUTRA-prompt button... No approved exceptions exist." Acceptance (superseding the original DOM-scan-for-zero-sliders acceptance): a headless DOM scan confirms every slider on every listed surface has a paired numeric input beside it, both inputs visibly showing metric+imperial where the parameter is a length/area; changing either the slider or the numeric input produces the identical model mutation, verified on the deployed edge; SUTRA's own text/voice/chip path is unaffected and still fully functional alongside the paired controls. **Reinforced by W-85 (2026-09-07):** on the LandIntel/Land-tab/Territory surfaces specifically, categorical/discrete choices (never a genuine numeric range) render as chips/selects, not sliders; every remaining numeric slider on those surfaces additionally shows a live value readout + units at all times, not just on interaction — a stricter application of this row's existing pairing requirement, not a new mechanism. | W-24, W-28, W-30 | READY |

## Notes

- **PRODUCT tagging (2026-09-08, RULE 57), scope of this pass:** tagged
  the 22 rows actively named in this pass's assignment sequences
  (W-66/67/69/70/72/73/74/79b/79d/79e/79g/80/83/85/87/88/90/91/92/94/95,
  plus the newly-seeded W-97) — every other pre-existing row on this
  board (roughly 70 rows) does NOT yet carry a PRODUCT tag and should
  not be assumed CROSS or any other value until actually tagged; a
  future pass backfills the remainder incrementally, per RULE 57's own
  "incremental, not retroactive" framing. **W-86 flagged, not
  fabricated:** the operator's MASON sequence names "W-86" as a step,
  but no W-86 row exists anywhere on disk — no seeding commit, no
  authored deliverable found (checked directly, same method as the
  earlier W-50/90/93 recovery). Recorded on docs/seats/MASON.md as a
  skip-and-wait step, not silently dropped from the sequence or
  renumbered to the next real row.
- **ASSIGNEE clarification (2026-09-08, RULE 56 OPERATOR_INTERFACE):**
  the "Eligible seats" column IS this board's ASSIGNEE column — a
  single named seat there is that row's assignee; "(any seat —
  owner-agnostic)" is itself a valid, explicit assignee designation,
  not an absent one. No retroactive schema change was made to any
  existing row; this note documents the mapping RULE 56 requires. Per
  RULE 56, operator corrections on a seat's work now arrive here as a
  new/amended assignment row (assignee + acceptance), not as a direct
  prompt to that seat.
- **Board reconciliation (2026-09-08), per PI's first Execution Ledger
  review:** W-22, W-23, W-24, W-25, W-37, W-38, and W-39 were cited as
  seeded rows in docs/ACTIVITY_LOG.md but had gone missing from this
  board — a real gap from this session's repeated branch-rebuild/
  cherry-pick cycles (the same class of incident logged earlier this
  session for RULE 42/43 and other content), not intentional omissions.
  Each was restored from its original seeding commit, re-verified
  against current disk state before restoring (not blindly copied):
  W-22 is superseded by the later, more complete W-92 PHOTOREAL_VIEWPORT
  and marked SUPERSEDED rather than restored as independently pull-
  eligible; W-23 remains a valid, un-superseded ROADMAP-LABEL; W-24 is
  restored as READY and flagged as the most consequential recovery
  (a dozen+ other rows depend on it with nothing to point to until
  now); W-25 is restored but flagged with a real, unresolved conflict
  between its original two-new-tab assumption and the since-established
  fixed ten-tab structure; W-37/38/39 are restored verbatim with
  cross-reference notes against newer overlapping rows (W-69, W-89)
  rather than treated as stale duplicates. **Follow-up (same day):**
  PI's own Cycle 1 Execution Ledger review (see docs/EXECUTION_LEDGER.md)
  found three more real, landed-or-authored gaps this first pass missed:
  W-50 HARNESS_24x7 (real, landed CRANE work, `fcfc892f`, never seeded
  through this board — a governance gap flagged but left unresolved
  earlier this session), W-90 LAND_DATA_UNIVERSE, and W-93
  ARCHITECT_PREDESIGN_FACTS (both real, landed ATLAS research documents,
  `4e905227b`/`363fc14f4`, likewise never boarded). All three seeded:
  W-50 retroactively as DONE (the work is real and landed); W-90/W-93 as
  READY implementation rows deriving their scope directly from the
  research documents' own "Needs KB row" findings, not invented beyond
  them. The remaining numbers (W-15, 18, 19, 21, 35, 36, 49, 56, 57, 62,
  86) were checked against `git log --all --grep` and found to have NO
  seeding commit and no authored deliverable under any branch name — not
  gaps, just numbers never used (W-36 specifically was already flagged,
  in W-51's own notes, as tracing to MASON's own branch-naming
  convention, never a SCRIBE-seeded row). This board remains the single
  source of truth PI's ledger audits against — a future numeric gap
  should be checked the same way (activity-log citation search +
  disk-state check + a search for any authored-but-unboarded deliverable,
  not just seeding commits) before being assumed intentional — this
  session's own W-50/90/93 miss on the first pass is the reason to check
  for authored docs, not only board-seeding commits, going forward.
- Envelopes are deliberately scoped to avoid overlap per RULE 35(2)'s
  claim rule; CRANE holds `lib/types.ts` (W-03) as the sole editor of
  that shared contract file until it marks W-03 DONE, per RULE 35(5) —
  RIVET and MASON's rows do not touch it.
- W-01 was already CLAIMED (CRANE) at seeding time per the operator's
  instruction — not a claim SCRIBE made on CRANE's behalf beyond
  recording what was stated.
- No row here supersedes docs/WAVE_QUEUE.md; it remains the permanent
  ledger of record. This board is the pull-queue mechanism RULE 35
  layers on top of it while RULE 34's Workspace focus is in effect.
- W-11..W-14 (added 2026-09-04 per AGENTS.md RULE 36's observe-refine
  loop) are operator live-site observations, converted directly to
  board rows without a seat relay, per RULE 36(1). Each DONE among
  them gets a docs/TASK_REPORTS.md entry per RULE 36(3), same as every
  other row.
- W-16 (added 2026-09-04, same RULE 36 mechanism) restores a tool a
  sweep actually removed — see AGENTS.md RULE 29's Feature Conservation
  addendum and docs/LIVE_TOOLS_REGISTRY.md. Assigned RIVET (the
  operator's instruction allowed MASON or RIVET; SCRIBE assigned RIVET
  as the seat already carrying similar UI-restoration work on W-11).
  W-15 is intentionally unused here — not seeded by this instruction,
  left open for the next observation.
- W-20 (added 2026-09-04) exists to resolve a MASON-vs-CRANE
  build-vs-reuse disagreement on IFC export before either side writes
  more code — assigned CRANE since the gate tests CRANE's own existing
  file; W-06 (ExportBar, MASON) is downstream and blocked on this
  row's result, not touched by it directly.
- **W-18, W-19, W-21 do not exist as seeded rows** as of 2026-09-04,
  despite being referenced by ID in later instructions (W-22's original
  dependency list named W-21; docs/UX_FLOW.md was asked to be linked as
  an acceptance reference for W-19 and W-21 specifically). SCRIBE has
  not invented scope for any of the three to fill the gap — flagged
  here, on W-22's row, and in docs/UX_FLOW.md's own Notes section
  instead. Whoever defines W-18/W-19/W-21 should seed them for real and
  update W-22's Deps column and UX_FLOW.md's references once they
  exist.
- W-22 and W-23 (added 2026-09-04) were seeded across two messages —
  the first gave the row content and an acceptance criterion citing a
  reference image file; a follow-up message from the same operator
  amended W-22's acceptance to drop that file dependency before this
  board was pushed. The row above reflects the amended (final)
  acceptance only; the withdrawn reference-file version was never
  committed.
- W-24/W-25 (added 2026-09-04) were seeded with a choice of eligible
  seat ("CRANE or MASON" / "RIVET or MASON") left open by the
  operator, per this board's own convention of not force-assigning
  when the instruction itself offers a choice — whichever seat pulls
  first per RULE 35(2) claims it.
- W-26 (added 2026-09-04) is deliberately placed as this table's
  literal first row and carries a non-standard Status value
  ("READY — PRIORITY-JUMP", mirroring docs/WAVE_QUEUE.md's existing
  PRIORITY-JUMP convention, e.g. W2-375) because the operator stated
  explicitly that nothing outranks it. CRANE should claim and pull
  this row ahead of any other CRANE-eligible READY row, including
  ones with an earlier ID. **Update (this push):** a commit matching
  W-26's exact envelope (`86ef5791`, "[land:crane/w26-cockpit-route-flip]")
  is present on `origin/main` as of this push — SCRIBE has recorded
  this as a verifiable fact per RULE 40, but has not itself checked the
  deployed edge, so the row's Status column reads the commit fact only,
  not a DONE claim.
- W-27/W-28 (added 2026-09-04) make the command bar/option-chip flow
  the cockpit's primary interaction model, demoting manual sliders to
  an Advanced surface rather than removing them from the app entirely.
  Both cite docs/UX_FLOW.md's rewritten Phase (4) as their design
  reference. W-27 has a split envelope (MASON UI, CRANE grammar) — each
  seat's half is its own claim under RULE 35, not a joint claim.
- W-29/W-30/W-31 (added 2026-09-04) ground the conversational/guided
  flow in real, versioned, cited data rather than free-floating
  assistant text — W-31 is explicitly gated on a not-yet-made LLM-seat
  approval decision and stays ROADMAP-LABEL until that happens.
- W-32 (added 2026-09-04) is an umbrella row over three real
  sub-rows (W-32a/b/c), each its own claimable envelope — see the flag
  on W-32's own row: the underlying ATLAS 8-step battery (W-10) has no
  logged run/results on disk as of this writing, only the three
  failing check names given directly in the instruction that created
  this row. Whoever ran the battery should log the full W-10 results
  for real, separately from this fix work.
- W-33 (added 2026-09-04) is a three-way split envelope (RIVET's card
  UI, CRANE's route/panel/pre-seed, MASON's 3D pre-seed wiring) — each
  seat claims its own piece under RULE 35, not a joint claim. It also
  updates the cockpit's region law (docs/UX_FLOW.md phase 3) to add a
  sixth region, the left-edge side panel, present only when a project
  has a source parcel.
- W-34 (added 2026-09-04) implements AGENTS.md RULE 41's hard device/
  perf gate as actual CI infrastructure, not just documented budgets.
  It retroactively adds a perf-delta acceptance clause to W-27, W-28,
  and W-33's rows (each blocks its own landing on a regression against
  `budgets.json` once W-34 exists) — every other currently-open row
  gets the same clause the next time SCRIBE touches it, per RULE
  41's blanket applicability.
- W-35a/W-35 (added 2026-09-04): W-35a is research/report only, no
  code envelope, and its verdict (adopt-as-dep / adopt-pattern /
  reject) is CRANE's to reach from real inspection, not pre-decided by
  this row's text. SCRIBE has NOT independently confirmed
  `img2threejs/img2threejs` exists or resolves — the repo name is
  recorded as given, and CRANE's own fetch is the first real
  verification per RULE 39/40. W-35 is written so MASON isn't fully
  blocked on W-35a's outcome: the entry-screen UI and guided-checklist
  wiring don't depend on which reconstruction method wins, only the
  actual mesh-generation call does.
- W-37/W-38 (added 2026-09-04) are both RIVET rows with an explicit
  sequencing instruction from the operator: W-37 lands first, named
  as the visible defect. W-38's Deps column lists W-37 to make this
  order a real RULE 35 dependency, not just a stated preference.
- W-39 (added 2026-09-04) is a split envelope (RIVET UI, MASON
  demo-mode 3D) touching nearly every marketing route at once —
  verified the "Start Free Trial" CTA text is real on multiple actual
  page files before citing it as the reference point, rather than
  assuming it. ATLAS's honesty-check on the copy (item 5) is a real
  gate on this row's DONE status, not an optional courtesy pass.
- **W-39/W-38 re-sequenced (2026-09-04):** the operator moved W-39
  ahead of W-38 in a follow-up message ("the operator asked where the
  link is; it must be visible this sequence") — the table row order
  and W-38's Deps column both reflect the new order; the original
  W-37-before-W-38 requirement is unchanged and still holds.
- W-40/W-41/W-42 (added 2026-09-04): W-40 is a split envelope
  (MASON+RIVET) that changes the cockpit's region law for a second
  time (after W-33's left-panel addition) — docs/UX_FLOW.md phase 3
  updated to match. W-41 extends the existing W-29 knowledge base
  rather than replacing it — CRANE should treat this as additive.
  W-42 is UI-only, no new data path — it reuses W-33's existing
  record-card structure with sample values swapped in for real ones.
- W-43/W-44/W-45 (added 2026-09-04, W-43 amended same day with the
  ONE-LIBRARY note before any consumer began reading it): W-43 is the
  single source of truth for commercial/technical/parametric materials
  data — DesignStudio, Structura, and BOQ (W-44) all read it, none
  maintain their own copy. W-44 explicitly reads W-43 only, per the
  amendment. W-45 is roadmap/post-launch as given — its adapter
  contract (`price_source` field) is defined now inside W-43's schema
  specifically so no later migration is needed to add it.
- W-46 (added 2026-09-04) is a separate rates/pricing engine from
  W-29/W-43's knowledge-base and materials-catalog work — it has its
  own status-chip vocabulary (VERIFIED-PUBLIC/REFERENCE-ONLY/CONCEPT/
  LIVE) rather than reusing VERIFIED-SAMPLE/INDICATIVE, since it's a
  distinct data class (streamed rates, not code-facts or catalog
  items). Per AGENTS.md RULE 43, seeded here as SCRIBE's own docs-only
  work — this row must land on `origin/main` before any downstream
  relay cites "W-46" to a seat.
- W-47/W-48 (added 2026-09-04, MASON): W-48 explicitly supersedes
  part of W-27's own acceptance criteria (sliders-to-Advanced becomes
  registry-controls-visible-by-default) — W-27's row text was amended
  in place to reflect this rather than leaving a stale, contradicted
  acceptance line standing. W-47 depends on W-40/W-33 since it
  restructures panels those rows introduced.
- **Status reconciliation pass (2026-09-04):** SCRIBE searched
  `origin/main`'s commit log for direct evidence of each row's actual
  landing (explicit `[task:W-NN]` tags, branch names matching a row's
  own ID, or diff content matching a row's stated envelope) and flipped
  W-01, W-08, W-17, W-26, W-27, W-28, W-32/32a/32b/32c, and W-46 to
  DONE with their real landing SHAs. **This is not a claim that every
  other row is confirmed still unlanded** — it is the set SCRIBE found
  clear evidence for in this pass; every DONE row above cites its own
  SHA so the claim is independently checkable, per RULE 40. Rows left
  READY either have no landing evidence found, or (per W-20's existing
  entry) have their own more specific status already recorded. None of
  these DONE marks are a RULE 25 live-edge verification — that remains
  a separate, open concern for whichever seat/ATLAS confirms it.
- W-51 (added 2026-09-05) is a split envelope (MASON 3D rendering,
  CRANE source ToS due-diligence). CRANE's due-diligence gates the
  actual default source choice — this row does not pre-select Bhuvan/
  Esri/EOX as final, only as the compliance-ordered candidate list to
  evaluate in that order. Honesty chips (attribution, imagery-date,
  OSM-footprint, offline-placeholder) are all required, not optional
  polish — ATLAS's audit gains a standing check for this row
  specifically.
- **Note on independent row numbering (2026-09-05):** `origin/main` now
  carries at least one row (`crane/w50-harness-24x7`) seeded outside
  this board/SCRIBE's own numbering. Flagged, not resolved here — this
  board's own numbering continues from its own last-used ID (W-51) per
  the operator's explicit instruction; reconciling the two numbering
  sequences is a separate question for the operator/conductor.
- **W-51 amended to ONE-GROUND (2026-09-05), applying AGENTS.md RULE 44
  to itself as the worked example:** the operator's original correction
  named only the Land tab; RULE 44 required generalizing that principle
  (a real ground belongs wherever a cockpit view renders a site) to
  every analogous surface — all 10 tabs plus every product-page preview
  — rather than leaving the fix scoped to the single named tab. This
  row is the first row this session amended specifically because RULE
  44 required it, not because of a separate operator follow-up.
- W-52 (added 2026-09-05) applies RULE 44's enumeration explicitly:
  the instruction itself named "the cockpit AND every product-page
  preview (all ten, per RULE 44)," so this row was seeded already
  generalized rather than needing a later amendment pass. Amended
  W-47's row text to note the layering relationship (W-47 = maximized
  viewport, W-52 = the actual fullscreen control + render-profile
  switch on top of it) rather than leaving two overlapping-sounding
  rows unexplained.
- W-28's acceptance amended 2026-09-05 (RULE 44, generalizing SUTRA's
  guided-tree/KB-answer/distinct-intent requirements across every
  route/preview per W-40): flagged explicitly that this row's existing
  DONE status reflects the pre-amendment landing only — the amended
  acceptance items are not yet independently verified.
