# TECH_SCOUT.md — W-68 TECH_SCOUT

Live GitHub research, each target independently verified (exists, license, last activity) via `gh api repos/<owner>/<repo>` — not from memory or a search-result snippet alone. Snapshot taken 2026-09-06. Per the row's own framing: **nothing here is "copy as-is"** — every mapping below extracts a concept and re-implements it as a Ferrum-native feature, gated by license and effort.

## Verification + Decision Table

| # | Target | Verified? | License | Last activity (pushed) | Ferrum feature mapping | Mode | Effort |
|---|---|---|---|---|---|---|---|
| 1 | `openai/whisper` | ✅ exists, 108.5k★ | MIT | 2026-08-31 | SUTRA voice-to-text — fixes the Safari gap: `SutraPanel.tsx` currently depends on `SpeechRecognition`/`webkitSpeechRecognition`, which is unreliable/absent on some Safari versions and shows an honest "Voice input is unavailable here" fallback there today. Whisper's model (or a hosted Whisper API) gives a server-side transcription path independent of any browser's native speech API. | **Adapt** | Moderate — needs a new Worker route accepting an audio blob, a model-hosting decision (self-run vs. hosted API), and a SUTRA-side fallback path when `webkitSpeechRecognition` is absent. Not a client-side drop-in. |
| 2 | `plausible/analytics` | ✅ exists, 28.9k★ | AGPL-3.0 | 2026-09-04 | Self-hosted, cookie-free, privacy-first analytics for the marketing site + workspace — replaces having no analytics at all (none currently found on disk). | **Use-as-is** (deployed as a separate self-hosted service, not embedded into this repo's codebase) | Low — deploy the existing Docker image, add the tracking snippet. AGPL-3.0 governs modifications to Plausible's own source, not the site being measured; using it unmodified as a separate hosted service carries no copyleft obligation on Ferrum OS's own code. |
| 3 | `penpot/penpot` | ✅ exists, 59.6k★ | MPL-2.0 | 2026-09-04 | Vector plan-editor concept for DesignStudio's 2D plan view (`Space3D.tsx`'s "Plan/Front/Side" tabs currently render fixed geometry, no direct-manipulation vector editing). | **Concept-only** — Penpot is a full general-purpose design tool; embedding it whole is disproportionate to a plan-view editor. Borrow its selection/handle/snap-to-grid interaction patterns, not its code. | High if any code were adapted; low as a design-pattern study |
| 4 | `n8n-io/n8n` | ✅ exists, 203.5k★ | Custom "Sustainable Use License" (GitHub reports `NOASSERTION`) | 2026-09-06 | KB/rate-refresh jobs (re-pulling government reference rates, regulatory corpus updates) and fleet job automation (the 24/7 multi-agent harness's own scheduled tasks), replacing ad-hoc scripts. | **Adapt** — self-host n8n as the job-runner/orchestrator, wire Ferrum-specific workflow nodes; do not fork n8n's own code. | Moderate — infra deploy + workflow authoring. **License caution:** n8n's Sustainable Use License restricts building a competing commercial SaaS product on top of it; self-hosting for internal automation (not resold) is within its terms, but this should be confirmed against the license text before committing, not assumed. |
| 5 | `bitwarden/clients` | ✅ exists, 13.8k★ | `NOASSERTION` on GitHub (Bitwarden's actual license is GPLv3 for most client code) | 2026-09-05 | Secrets management + a client-side encrypted document-vault concept (e.g. for a future feature storing sensitive project documents client-side-encrypted). | **Concept-only** — the end-to-end encryption architecture (zero-knowledge vault, client-side crypto before upload) is the reusable idea; embedding actual Bitwarden client code into a construction-SaaS product is out of scope and its GPLv3 lineage would impose copyleft obligations on anything that statically links it. | High if any code adapted; the concept itself is a solid reference architecture |
| 6 | `cathrynlavery/diagram-design` | ✅ exists, 31.8k★ | MIT | 2026-09-03 | Auto-generated architectural/technical diagrams — could back a "explain this building" or system-architecture diagram feature (e.g. auto-diagramming a project's massing/compliance chain for a report), and directly overlaps with this very report's own diagramming needs. | **Adapt** — its self-contained HTML+SVG diagram-generation approach (no heavy charting library) fits a static-export site well. | Low–moderate |
| 7 | `tt-a1i/archify` | ✅ exists, 49.7k★ | MIT | 2026-09-06 | Overlaps #6's space (architecture/workflow/sequence diagram generation for verifiable, exportable diagrams) — evaluated as the "archviz concept" target, though its actual scope is diagram generation, not architectural visualization/rendering in the 3D sense. | **Concept-only** — redundant with #6 as a code source; worth noting only as a second data point on the same diagram-generation pattern, not a separate integration. | N/A (folds into #6's effort) |
| 8 | `PrimeIntellect-ai/protocol` (org verified; `prime-diloco` also relevant, 855★) | ✅ exists, 139★ (protocol) / 855★ (prime-diloco) | Apache-2.0 | 2025-11-10 (protocol) / 2026-07-17 (prime-diloco) | Distributed/decentralized LLM inference infrastructure — relevant to `W-31 GROUNDED_LLM_LAYER` (the retrieval-grounded language layer over SUTRA's deterministic engines), which is explicitly `ROADMAP-LABEL` pending a not-yet-approved trial. | **Concept-only**, and gated — no work should start here until W-31's underlying LLM-seat approval exists, per that row's own status. | N/A until W-31 gate clears |
| 9 | `ag-ui-protocol/ag-ui` | ✅ exists, 15.7k★ | MIT | 2026-09-04 | Standardizes the SUTRA ↔ workspace event protocol. Today, `SutraPanel.tsx` communicates via a single bespoke `onSubmit(text: string)` callback prop into `cockpit/page.tsx`'s `handleCommand` — no structured event schema, no standard for streaming partial responses, tool calls, or state deltas. | **Adapt** — replace the bespoke callback with AG-UI's event-schema conventions (structured events: text message, tool call, state delta) as SUTRA's guided-tree/command-bar work (W-27/W-58) grows. | Moderate |
| 10 | `braveopotatato` (repo/user as given) | ❌ **NOT FOUND** — no matching user, no matching repo, zero search hits | — | — | — | **Reject** | N/A — cannot map or license-check something that does not exist on GitHub under this name. If a different spelling/slug was intended, re-supply it. |
| 11 | `math-curve-loaders` (best candidate: `Paidax01/math-curve-loaders`) | ✅ exists, 2.0k★ | **No license file** (GitHub reports `null`) | 2026-04-04 | A gallery of mathematical-curve-based CSS/JS loading-spinner animations. No credible Ferrum feature mapping was found — this is a decorative loading-state component library, unconnected to any cockpit/SUTRA/engine surface. | **Reject** | N/A — no license means default "all rights reserved," making even concept-adaptation of its exact curve-easing code legally unclear; combined with no real feature fit, this doesn't clear the bar |
| 12 | `calcom/cal.diy` | ✅ exists, 48.2k★ | MIT | 2026-09-04 | "Book a consult" scheduling CTA — a real, current Cal.com product/repo for self-hostable scheduling infrastructure. Direct fit for a marketing-site or post-lead-capture consult-booking flow (no such flow currently exists on disk). | **Use-as-is** (embed/self-host the scheduling widget, not fork the codebase) | Low |
| 13 | "enviz-style" immersive 3D web setup | ⚠️ **No canonical repo found matching this description** — GitHub search for "enviz" returns unrelated projects (a pentest network-visualization tool, IBM's "Envizi" emissions-accounting product, an enrollment-visualization class project); none concern immersive 3D web scenes | — | — | If a specific reference site/repo was intended (e.g. a known three.js/WebGL "environment showcase" demo), it needs to be re-named/re-linked before this can be verified or mapped. As given, this is a **study topic**, not a verifiable GitHub target. | **Concept-only / unverifiable as named** | N/A |

## Summary by mode

- **Use-as-is (2):** `plausible/analytics` (self-hosted analytics), `calcom/cal.diy` (booking widget)
- **Adapt (4):** `openai/whisper` (voice-to-text), `n8n-io/n8n` (job automation), `cathrynlavery/diagram-design` (auto-diagrams), `ag-ui-protocol/ag-ui` (event protocol)
- **Concept-only (4):** `penpot/penpot`, `bitwarden/clients`, `tt-a1i/archify` (redundant w/ #6), `PrimeIntellect-ai/protocol` (gated on W-31)
- **Reject (2):** `braveopotatato` (does not exist), `math-curve-loaders` (no license, no feature fit)
- **Unverifiable as named (1):** "enviz-style" 3D setup study — needs a concrete reference before it can be scouted for real

## Method note

Every "exists / license / last activity" cell above came from a live `gh api repos/<owner>/<repo>` call against the real GitHub API in this session, not a cached search snippet — ambiguous slugs (`cathrynlavery/diagram`, `archify`, `math-curve-loaders`, `cal.diy`, `enviz`) were first resolved via `gh search repos`, then the winning candidate was independently re-verified via a direct `repos/` API call before being entered into the table. Two targets (`braveopotatato`, "enviz-style") did not clear that bar and are marked accordingly rather than guessed at.
