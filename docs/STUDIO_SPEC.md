# Studio specification

## Status: source gate

The required seven-point operator vision was not included with this task and
is absent from the tracked repository and available worktrees at the time of
authoring. This document must mirror that text verbatim; it must not replace
the missing source with an inferred or paraphrased product vision.

## Required source

The conductor or operator must supply the canonical seven numbered points in
their final wording. Once supplied, this section is to contain those seven
points unchanged, including order, headings, punctuation, and qualifiers.

## Integration boundary

Until the source is supplied:

- no Studio UI, API, workflow, acceptance test, or marketing claim may treat
  an inferred seven-point list as approved;
- `docs/VAASTU_RULESET.md` is the source of truth for S3/S4 orientation and
  placement preference checks only; and
- `docs/OSM_NEIGHBOUR_PIPELINE.md` remains a proposal, not a data integration.

**Resolution:** replace this source-gate note with the supplied seven points,
verbatim, in one reviewable update.
