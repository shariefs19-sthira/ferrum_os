# RESUME_SCRIBE.md — Living resume (AGENTS.md RULE 21(3))

Updated every turn by SCRIBE. After a limit event or API error, the next
SCRIBE session reads this file FIRST, before anything else, and resumes
exactly from what it says — not from a reconstructed summary of chat
history.

## Done (this turn, with SHAs)
- Added AGENTS.md RULE 21 (self-verifying tools + living resume) and
  created the four RESUME_<SEAT>.md templates (CRANE, ATLAS, CODEX,
  SCRIBE) — commit pending push, see docs/ACTIVITY_LOG.md's matching
  entry for the exact SHA once pushed.

## In-flight
- Mirroring RULE 21 into docs/seats/*.md (same branch, same turn).

## Next planned step
- Populate this file with real done/in-flight/next/blockers state on
  every subsequent SCRIBE turn, per RULE 21(3) — this first entry is the
  template's own creation, not yet a real working-state snapshot.

## Current blockers
- None. Known outstanding cross-branch item: the CODEX→MASON/RIVET
  rename (W2-356) still hasn't reconciled with the later w2-359+ branch
  chain — see AGENTS.md/docs/ROLE_MAP.md change log and the W2-357
  landing-pipeline fix this is meant to eventually prevent recurring.

## Last updated
- 2026-09-03, this turn.
